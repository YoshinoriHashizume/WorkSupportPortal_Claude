# ユビキタス言語集（在庫発注アラート）

文書ID: UL-INVENTORY-ORDER-ALERT-2026-001
作成日: 2026-07-30
更新日: 2026-09-21
対応文書: [ubiquitous_language_core.md](../../../docs/ubiquitous_language_core.md)（UL-CORE-2026-001）、[在庫発注アラート_機能仕様書.md](./在庫発注アラート_機能仕様書.md)（REQ-INVENTORY-ORDER-ALERT-2026-001）、[requirements.md](./spec/02_low-flow-visibility/requirements.md)（REQ-LOW-FLOW-VISIBILITY-2026-001）

本書は **生産管理（在庫発注アラート）** コンテキスト固有の用語を定義する。
プロジェクト横断の用語は [ubiquitous_language_core.md](../../../docs/ubiquitous_language_core.md) を正とし、本書では重複定義しない。
「使用しない表現」の適用範囲の考え方は core に従う。

---

## 1. エンティティ

### E-201: 確認記録

- **英語名**: `Confirmation`
- **定義**: 得意先 × 得意先品番（core V-005）ごとに、仕入先への確認状況を記録したもの。確認状態（S-201）・確認者・確認日時・確認時のアラートレベル（S-202）を保持する。
- **構成要素**: 保存キーは **得意先コード + 得意先品番**。
- **使用しない表現**: 「処置記録」「対応履歴」「チェック記録」
- **補足**: ユーザー単位ではなく **全員で共有** する。実装は `InventoryOrderAlertConfirmation`。

### E-202: 確認メモ

- **英語名**: `ConfirmationMemo`
- **定義**: 確認記録（E-201）に時系列で追記する記録。仕入先への確認内容・回答概要・発注可否等を残す。
- **構成要素**: 内容（最大 500 文字）・登録者・登録日時。
- **使用しない表現**: 「コメント」「備考欄」「ノート」
- **補足**: **追記のみ**（編集・削除は対象外）。確認状態（S-201）の変更ではメモ履歴に触れない。実装は `InventoryOrderAlertConfirmationMemoEntry`。

---

## 2. バリューオブジェクト

### V-201: 最終入荷日

- **英語名**: `LastIncomingDate`
- **定義**: 内作品番（core V-006）と仕入先（V-203）の組み合わせにおける、最も新しい入荷実績日（`T_PAST_INSPC_ACPT.ACPT_DATE` の最大値）。
- **構成要素**: 日付。**入荷実績がない場合は空**。
- **使用しない表現**: 「最終仕入日」「最終仕入れ日」「最終受入日」
- **補足**: 空の場合、アラート判定ではすべての出荷を「最終入荷日以降の出荷」として扱う。
  **本項目は MARI の入荷実績（検収）に基づく**。SLIMS 在庫内訳（V-205）の入荷日（`WNYUDT`）は性質が異なるため、両者を同一視してはならない（V-205 の補足を参照）。

### V-202: 最終出荷日

- **英語名**: `LastShipmentDate`
- **定義**: 得意先 × 得意先品番における最も新しい出荷実績日（`T_SHIP.SHIP_DATE` の最大値、`DEL_FLG != 1`）。
- **構成要素**: 日付。出荷実績がない場合は空。
- **使用しない表現**: 「最終売上日」「直近出荷日」

### V-203: 仕入先

- **英語名**: `Supplier`
- **定義**: BOM 階層 1 の外注構成子を供給する取引先。`M_PUCH_UNIT_COST_H` と `M_VEND_CTRL` から解決する。
- **構成要素**: 仕入先コード・仕入先名（`VEND_ANAME`）・仕入先品番。
- **使用しない表現**: **「仕入れ先」「発注先」「ベンダー」「子取引先」**（子取引先は検収書比較の別概念）

### V-204: ロケーション

- **英語名**: `StockLocation`
- **定義**: SLIMS（core SYS-004）在庫 CSV が持つ在庫の保管位置（`WLOCCD`）。`[0-9][0-9A-Z][0-9]-[0-9]{2}-[0-9]` 形式の行のみ在庫集計の対象とする。
- **使用しない表現**: 「棚番」「置場」「保管場所」
- **補足**: 一覧・CSV には列として出力せず、在庫数の合算と在庫内訳（V-205）の表示にのみ用いる。

### V-205: 在庫内訳

- **英語名**: `StockLocationDetail`
- **定義**: 得意先品番ごとの在庫を、SLIMS 在庫 CSV の行単位（ロケーション・在庫数・入荷日）で集約せずに並べたもの。
- **使用しない表現**: 「在庫明細」「ロケーション別在庫」
- **補足**: 在庫内訳の入荷日（`WNYUDT`）は SLIMS の値であり、最終入荷日（V-201）と一致しない場合がある。
  同一ロケーションの行が複数あっても集約せずそのまま表示する。**行の集合であるが、識別子を持たず値の集合として扱う**ためバリューオブジェクトに分類する。
- **重要（2026-09-11 追記・業務ヒアリング）**: **SLIMS の入荷日つきの各行は「そのロットの残数」であり、入荷実績の履歴ではない。**
  出荷（払い出し）のたびに当該ロットの数量が減る。例: 10/4 に 1,000 個入荷（棚入れ）し、そこから 50 個出荷すると、**10/4 の行は 950 個になる**。
  したがって次の点に注意する。
  - **残数 0 になったロットは行ごと消える**。「入荷日 X のロットが無い」ことは「その日に入荷しなかった」ことを意味せず、「入荷したが全量払い出された」場合も同じ見え方になる。
  - 在庫内訳から**入荷量・入荷回数を読み取ることはできない**。入荷の実績は MARI（入荷推移 V-217・最終入荷日 V-201）で見る。
  - 古い入荷日のまま残っている行は、**その日から動いていない在庫**（滞留）を示す。
  - 在庫内訳の合計は SLIMS 在庫数（V-206）と一致する（いずれも現時点の残数であるため）。

### V-206: SLIMS 在庫数

- **英語名**: `SlimsStockQuantity`
- **定義**: SLIMS（core SYS-004）在庫 CSV の **引当可能総バラ数**（`WKSBQT`）を、得意先品番（core V-005）単位で合算した数量。**実在庫の正**として扱う。
- **構成要素**: 小数を許容する数量。単位は **バラ数**。SLIMS に一致する品番がない場合は **空**（0 ではない）。
- **使用しない表現**: 「在庫」（単独。在庫という概念全体を指すため区別する）「在庫バラ数」「引当数」「在庫数」（単独。MARI 在庫数 V-215 と区別できないため）
- **補足**: 流動区分（S-203）の判定には **用いない**（判断材料として表示するのみ）。ロケーション（V-204）の形式条件を満たす行のみを合算する。
  共通品（複数の得意先品番が同一品番に紐づく場合）では **同一の数量が複数行に現れる**。得意先ごとの按分はできないため、一覧の値を単純合計しても総在庫にはならない。
- **改定（2026-08-31）**: MARI 在庫数（V-215）を併記するようになったため、呼称を **「在庫数」から「SLIMS 在庫数」へ改めた**。出所を明示しない「在庫数」は使用しない。

### V-215: MARI 在庫数

- **英語名**: `MariStockQuantity`
- **定義**: 基幹 Oracle（MARI、core SYS-005）が持つ在庫数を **品番単位で合算**した数量。**判断材料**として SLIMS 在庫数（V-206）と併記する。
- **構成要素**: 数量。MARI に該当在庫がない場合は **空**（0 ではない）。0 と空は区別する。
- **使用しない表現**: 「基幹在庫」「システム在庫」「Oracle 在庫」（出所が曖昧、または内部実装の呼称であるため）
- **補足**: **実在庫の正は SLIMS 在庫数（V-206）** であり、本項目は差異に気づくための併記である。流動区分（S-203）の判定には用いない。
  取得は SLIMS 取込時のみで、一覧表示のたびに MARI へ問い合わせない。共通品での重複計上は V-206 と同じ性質を持つ。
  MARI 在庫を持たない既存の集計スナップショットでは **未取得** となり、画面では「－」を表示する。

### V-207: 担当者コード

- **英語名**: `SalesRepCode`
- **定義**: 得意先を担当する社内担当者のコード（`T_SHIP.CUST_CHRG_PSN_CD`）。得意先 × 得意先品番ごとに **最大値** を採用する。
- **構成要素**: 文字列。フィルタの候補は **数値として昇順**（非数字は末尾）に並べる。
- **使用しない表現**: 「営業担当コード」「担当コード」「担当者番号」
- **補足**: 一覧では得意先コードの **左** に表示する。出荷トレンド一覧にも同名の列があり、同一の項目を指す。

### V-208: 仕入先品番

- **英語名**: `SupplierItemCode`
- **定義**: BOM 階層 1 の外注構成子の品番。仕入先（V-203）が供給する品目を識別する。
- **構成要素**: 文字列。前方一致で絞り込める。
- **使用しない表現**: **「仕入れ先品番」**「外注品番」「構成子品番」
- **補足**: 一覧では仕入先名の右に表示する。得意先品番（core V-005）・内作品番（core V-006）とは別項目である。

### V-209: BOM 基準日

- **英語名**: `AsOfDate`（core V-008 の本コンテキストにおける呼称）
- **定義**: BOM の有効期間判定の基準となる日付。**SLIMS 在庫 CSV の取込日（サーバ暦日）** とし、集計スナップショットに固定保存する。
- **構成要素**: 日付。再取込するまで変わらない。
- **使用しない表現**: 「基準日」（単独）「取込基準日」「集計基準日」（集計基準日は出荷トレンド一覧の呼称）
- **補足**: 一覧画面には表示せず、在庫時点（T-202）のラベルで代替する。アラートレベル（S-202）の「出荷なし」の経過月数判定にも本日付を用いる。流動区分（S-203）の判定期間（V-211）の起点にも本日付を用いる。

### V-210: 判定軸

> **［2026-09-15 廃止］** 判定期間（V-211）に一本化した。「1 回の検索で死蔵と低流動を同時に見たい」という要望と、目的（仕入先が設備・金型を廃棄する前に気づく）から時間スケールを年単位に統一したため。以下は廃止前の定義（履歴として保持）。

- **英語名**: `FlowAxis`
- **定義**: 流動の乏しさを **どの時間スケールで見るか** を表す区分。**低流動判定軸** と **死蔵判定軸** の 2 値。一覧はどちらか一方を選んで表示する。
- **構成要素**: `low_flow`（低流動判定軸）/ `dormant`（死蔵判定軸）
- **使用しない表現**: 「判定基準」「時間軸」「スパン」「粒度」
- **補足**: 切り替えても集計スナップショットは作り直さず、**表示時に再判定する**（警告条件 T-203 と同じ扱い）。

### V-211: 判定期間

- **英語名**: `EvaluationPeriod`
- **定義**: 入荷または出荷が止まってから「対応が必要」と判断するまでの年数。**1 年 / 3 年 / 5 年** の固定 3 値、既定 **1 年**。この年数を境界として流動区分（S-203）を判定する。
- **構成要素**: 年数（1 / 3 / 5）。利用者は一覧のヘッダで切り替える。任意の値は指定できない。
- **使用しない表現**: 「判定軸」「低流動判定軸」「死蔵判定軸」「対象期間」「集計期間」「閾値」「警告月数」「境界年」（単独）
- **補足**: 起点は BOM 基準日（V-209）。「判定期間内」とは、基準日から判定期間だけさかのぼった日以降を指す。警告条件（T-203、1〜36 か月の設定値）とは **別概念**である。
  **メニューのアラート帯・確認記録の深刻化判定は既定の 1 年で固定判定**する（旧「低流動判定軸・3 か月」から変更）。

  **期間判断の基準（2026-09-21 統一。ユーザー判断「基準は一つにしたい」）**
  本機能で **期間にかかわる判断はすべて判定期間で行う**。値は次の 1 つの規則で決まる。

  - 判定期間の選択 UI がある画面（一覧）: **利用者が選んだ値**
  - 選択 UI がない場所（メニューのアラート帯、確認記録の深刻化判定）: **既定値 1 年**

  判定期間を使う判断は、期間内入荷（V-212）／期間内出荷（V-213）と、それらから決まる 在庫ありの 4 区分（S-203 ランク 2・3・4・6）、および流動区分の理由のうち「{判定期間}以上出荷なし・経路要確認」「内示あり（立ち上がり／出荷経路要確認）」である。
  在庫なしの 3 区分（欠品 T-209 の 2 区分・打ち切り候補 T-210）と、需要予測（V-220）・在庫切れ予測月（V-221）・在庫月数（V-222）・在庫切れリスク（S-204）は **期間を使わない**（在庫・需要・直近入荷・日数で決まる）ため、判定期間を切り替えても変わらない。
  判定期間とは**意味の違う窓**が 4 つあり、用途を固定している: **直近入荷の窓**（設定 `recentIncomingDays`・既定 30 日・範囲 1〜90。欠品 2 区分の振り分けのみ）、安全日数（設定・既定 14 日）、既定リードタイム（V-225。設定・既定 5 日）、監視期間（S-204。設定・既定 6 か月）。いずれも設定画面・設定 API で変更でき、判定期間のように画面で切り替えるものではない。
  年数を「年」単位にしたのは、目的が「仕入先が設備・金型を廃棄する前に気づく」ことであり、月単位の動きの鈍さは対象外とするため。
  > **［2026-09-15 改訂前］** 判定軸（V-210）ごとに、低流動判定軸は 1・3・6 か月、死蔵判定軸は 1・2・5 年の固定 6 値だった。

### V-212: 期間内入荷

- **英語名**: `RecentIncoming`
- **定義**: 最終入荷日（V-201）が判定期間（V-211）内にあること。
- **構成要素**: 真偽値。最終入荷日が **空の場合は「なし」** とする。
- **使用しない表現**: 「直近入荷」「入荷有無」「新規入荷」

### V-213: 期間内出荷

- **英語名**: `RecentShipment`
- **定義**: 最終出荷日（V-202）が判定期間（V-211）内にあること。
- **構成要素**: 真偽値。最終出荷日が **空の場合は「なし」** とする。
- **使用しない表現**: 「直近出荷」「出荷有無」
- **補足**: **最終入荷日以降の出荷（T-201）とは別概念**である。T-201 は最終入荷日を起点に数えるのに対し、本項目は基準日から判定期間だけさかのぼった日を起点とする。

### V-214: 入荷実績なし

- **英語名**: `NoIncomingRecord`
- **定義**: 判定期間（V-211）によらず、基幹 Oracle に入荷実績が **1 件もない**（最終入荷日 V-201 が空である）こと。
- **構成要素**: 真偽値。
- **使用しない表現**: 「未入荷」「入荷ゼロ」「新規品」「入荷なし」（期間内入荷 V-212 と紛れるため）
- **補足**: 旧アラートレベル（S-202）の **「重点」** が表していた条件。仕入先が設備・金型を廃棄していた事故事例（機能仕様書 §2.1）に直結するため、流動区分（S-203）とは **独立した副次フラグ** として供給リスク品の行に表示する。

### V-216: 出荷推移

- **英語名**: `ShipmentTrend`
- **定義**: 得意先 × 得意先品番（core V-005）ごとの、**月次出荷数量**（`T_SHIP.SHIP_QTY` の月別合計）を時系列に並べたもの。**推定在庫推移（V-218）の算出材料として用いる**（詳細ダイアログに独立した折れ線グラフとしては表示しない。2026-09-03、ユーザー指示により専用グラフ表示を撤去。DECISIONS.md参照）。
- **構成要素**: 年月（`YYYY-MM`）と数量の組を、**BOM 基準日（V-209）の月を含む直近 24 か月ぶん**、古い順に並べた固定長 24 件のリスト。出荷実績がない月も数量 0 として**必ず含める**（欠測として省かず、「その月は出荷が無かった」ことをグラフの連続性で示すため）。
- **使用しない表現**: 「在庫推移」「在庫変動」（在庫数そのものの時系列ではないため。SLIMS 在庫数 V-206・MARI 在庫数 V-215 は時系列を持たない）、「出荷実績履歴」
- **補足**: 推定在庫推移（V-218）の算出に用いる際は、**同一得意先品番の全得意先分を合算**する（2026-09-11 改訂）。データ自体は得意先 × 得意先品番の単位で保持する。
  SLIMS 取込時に、既存の出荷回数（T-201）と同じ Oracle 取得結果（`T_SHIP` 全件）から Python 側で月次に束ねて算出する。**Oracle への追加問い合わせは発生しない**。取込のたびに集計スナップショットへ保持し、一覧表示・詳細ダイアログ表示のたびには Oracle へ問い合わせない（V-206・V-215 と同じ方針）。品目の取扱い開始が 24 か月未満でも、window の先頭から 0 埋めする（固定長 24 件を保つ。値ではなく「観測期間」を表すため、開始前も 0 で埋めて問題ない）。

### V-217: 入荷推移

- **英語名**: `IncomingTrend`
- **定義**: 最終入荷日（V-201）と同じ突合キー（内作品番の BOM 階層 1 構成子・仕入先の組）ごとの、**月次入荷数量**（`T_PAST_INSPC_ACPT.INSPC_ACPT_QTY` の月別合計）を時系列に並べたもの。**推定在庫推移（V-218）の算出材料として用いる**とともに、**推定在庫推移のグラフに棒グラフとして重ねて表示する**。
  > **［2026/09/11 表示再開］** 2026-09-03 に「入出荷推移」区分（出荷・入荷の2系列折れ線）を撤去した経緯があるが、ユーザー指示「MARIの入荷の棒グラフを足して」により、**入荷推移のみ**を推定在庫推移グラフ上の棒グラフとして表示するようにした。**独立した区分・独立した折れ線グラフとしては復活させない**。出荷推移（V-216）は引き続き算出材料のみで、グラフには描かない。DECISIONS.md参照。
- **構成要素**: 出荷推移（V-216）と同じ形式（`YYYY-MM` と数量の組、直近 24 か月ぶん固定長 24 件、古い順、実績なしの月は 0）。
- **使用しない表現**: 「在庫推移」「在庫変動」（V-216 と同じ理由）、「受入推移」（`ACPT_QTY` 受入数量ではなく `INSPC_ACPT_QTY` 検収数量を用いるため。検収を経て正式に受け入れられた数量を指す）
- **補足**: 推定在庫推移（V-218）の算出・棒グラフ表示に用いる際は、当該得意先品番に紐づく **内作品番 × 仕入先の組の重複を除いて合算**する（2026-09-11 改訂。同じ組が複数行に現れるため、単純に行の値を使うと重複計上になる）。
  突合キーが **得意先 × 得意先品番ではない点が出荷推移（V-216）と異なる**。入荷は内作品番の BOM 階層 1 構成子（`level1_item_cd`）と仕入先（`level1_vend_cd`）の組で管理されるため（最終入荷日 V-201 の解決ロジックと同じ）。共通の内作品番を持つ複数の得意先品番では、同じ入荷推移が複数行に現れる（V-206 等と同じ重複計上の性質）。
  出荷推移とは異なり、**Oracle への追加問い合わせが 1 本発生する**（`T_PAST_INSPC_ACPT` から個々の検収明細を取得する必要があり、既存の `fetch_last_incoming_by_item_vend`（`MAX(ACPT_DATE)` のみを集計）を流用できないため）。取得は SLIMS 取込時の直近 24 か月分に絞り込む（`ACPT_DATE >= 取込月の23か月前` の WHERE 条件）。
  **表示は推定在庫推移（V-218）のグラフと同一の月軸・同一のY軸**に、ゼロを基準として上方向へ伸びる棒で描く（在庫数・入荷数量はいずれも**バラ数**で単位が同じため、軸を分けない）。棒は推定在庫の折れ線より背面に描き、折れ線を隠さない。

### V-218: 推定在庫推移

- **英語名**: `AnchoredStockTrend`
- **定義**: 出荷推移（V-216）・入荷推移（V-217）から逆算した、**参考値としての在庫数の月次推移**。現在の実在庫数（SLIMS 在庫数 V-206）を起点（直近月）とし、過去に向かって「当月末推定在庫 = 翌月末推定在庫 + 翌月出荷数量 − 翌月入荷数量」を24か月ぶん逆算して求める。
  **算出単位は「内作品番 × 仕入先の組を共有する得意先品番の集合」**とする（2026-09-11 改訂）。以下、この集合を **照合単位** と呼ぶ。
  在庫は得意先品番単位（V-206）、出荷は得意先 × 得意先品番単位（V-216）、入荷は内作品番 × 仕入先単位（V-217）と**3つの粒度が異なる**ため、3者を含む最小の閉じた集合まで広げないと数量が釣り合わない。
  照合単位は、**得意先品番と「内作品番 × 仕入先の組」を節点とする二部グラフの連結成分**として求める（1つの得意先品番が複数の組に紐づく場合、1つの組を複数の得意先品番が共有する場合の両方があるため）。
  | 要素 | 集計範囲 |
  |---|---|
  | 起点（在庫数） | 照合単位に含まれる**得意先品番ごとの SLIMS 在庫数（V-206）を、品番の重複を除いて合算**した値 |
  | 出荷数量 | 照合単位に含まれる**全行（全得意先 × 全得意先品番）を合算**した出荷推移（V-216） |
  | 入荷数量 | 照合単位に含まれる**内作品番 × 仕入先の組ごとに1回だけ合算**した入荷推移（V-217） |

  - 起点の合算では、在庫数が **該当なし（空）** の品番は **0** として加算する。**未取得（`－`）** の品番も 0 として加算するが、**照合単位内のすべての品番が未取得の場合は算出しない**（グラフを表示しない）。
  - 照合単位が**複数の得意先品番を含む場合、グラフの起点は詳細ダイアログの「在庫数(SLIMS)」の表示値と一致しない**（当該品番だけでなく集合全体の在庫合計になるため）。この場合は起点の内訳を画面に明示する。詳細ダイアログに、**SLIMS起点の1系列**を折れ線グラフとして表示し、**同じ月軸・同じY軸に入荷推移（V-217）を棒グラフとして重ねる**（在庫が跳ね上がった月の入荷実績を対比できるようにするため）。
  > **［2026/09/03 撤去済み］** 当初は SLIMS 在庫数 V-206 に加えて MARI 在庫数 V-215 を起点とする「MARI起点」系列も並べて表示していたが、ユーザー指示「MARI視点のグラフは削除して」により撤去した。理由の詳細は DECISIONS.md 参照。
- **構成要素**: 出荷推移（V-216）・入荷推移（V-217）と同じ形式（`YYYY-MM` と数量の組、直近 24 か月ぶん固定長 24 件、古い順）。数量は**マイナス値を許容する**（クランプしない。マイナスは「出荷・入荷の実績だけでは説明がつかないほど不足している」という危険信号としてそのまま見せる）。
- **使用しない表現**: 「在庫実績」「実在庫推移」（実測ではなく推定値のため）
- **補足**:
  - **実測値ではない**。棚卸差異・生産消費・返品など、出荷・入荷以外の在庫変動要因は反映されない。24か月遡るほど誤差が積み上がる可能性があるため、判断材料の一つとして扱う（流動区分の判定には用いない）。
  - **算出はクライアント（JS）側で行う**。出荷推移・入荷推移・SLIMS在庫数はいずれも既に配信済みのデータであり、算出のための新規 Oracle 問い合わせ・配信データ量の増加は発生しない。合算も配信済みの全行から行うため、同様に追加の通信は発生しない。
  - **合算は一覧のフィルタ・ソートの状態に依存させない**（絞り込み後の行ではなく、常に配信された全行で合算する）。物理的な在庫・入荷は画面の絞り込みと無関係のため。
  - **同一の照合単位に属する行はすべて同じグラフになる**。在庫数・入荷数量が元々複数行に共有されている（同じ値が複数行に現れる）ため、行ごとに異なるグラフを描くと数量の整合が取れない。
  - **［2026-09-11 改訂の経緯］** 当初は出荷を「当該得意先の分のみ」、入荷を「内作品番 × 仕入先の全量」としていたため、**共通品では入荷だけが過大に効き、構造的にマイナスへ沈んでいた**。実データ（2026-09-07 取込、2,298 行）でのマイナスを含む行数は、
    行単位 **443 行（19.3%）** → 得意先品番単位 **240 行（10.4%）** → 照合単位 **210 行（9.1%）** と段階的に減った。
    例: 得意先品番 96160-00500 は、同じ内作品番 96160-00500-9065（仕入先 9065）を得意先品番 10523-X0A02 と共有しており、入荷 40,000 に対し在庫が 12,970（96160-00500）と 19,000（10523-X0A02）に分かれていた。品番単位では -16,430 のマイナスが残るが、照合単位では `31,970 + 12,600 − 40,000 = +4,570` で釣り合う。DECISIONS.md ステージ14・15 参照。
  - **起点の在庫数が「該当なし」（SLIMS 取込済みだが SLIMS に当該品番が無い＝在庫数が空）の場合は、0 を起点として算出する**（2026-09-11 改訂。ユーザー指示「SLIMSに在庫がないものは0として履歴を作成して」）。在庫ゼロの品目でも過去に在庫を持っていた推移が見えるようにするため。
  - 一方、在庫数が**未取得**（`－`。SLIMS 未取込、または本機能導入前の旧スナップショット）の場合は **算出せず、グラフを表示しない**（SLIMS を参照できていない状態を「在庫ゼロ」と描いてはならないため）。
  - この 0 起点の扱いは**推定在庫推移の算出に限る**。SLIMS 在庫数（V-206）そのものの表示は「該当なし＝空」「未取得＝`－`」のままで、3 状態の区別は維持する。

- **予測部分（2026-09-18 追加）**: グラフの右側に、現在の在庫から内示（V-219）を引き、完成品直下の工程の発注残（V-224）を納期の月に加えた **予測在庫** を翌月〜翌々々月の 3 点、**点線**で示す。在庫切れ予測月（V-221）が範囲内なら縦線を添える。内示がない照合単位では予測部分を描かない（2026-09-18 改訂）。参考値であり判定には用いない。

### V-219: 内示推移

- **英語名**: `UnconfirmedOrderTrend`
- **定義**: 得意先 × 内作品番ごとの **月次内示受注数量**（`T_UNCNFM_ODR.UNCNFM_REQUIRED_QTY` を所要日 `UNCNFM_REQUIRED_DATE` の月別に合計、`DEL_FLG = '0'`）を、**当月残（BOM 基準日 V-209＝取込日以降）＋ BOM 基準日の翌月〜翌々々月の 3 か月** で並べたもの。
- **構成要素**: 年月と数量（バラ数）の組、固定 4 件（当月残 + 3 か月）、古い順。内示受注がない月は 0。
- **使用しない表現**: 「内示」（単独）「フォーキャスト」「受注見込み」「内示受注数」「内示受注数量推移」
- **補足**: SLIMS 取込時に Oracle へ 1 クエリ（`T_UNCNFM_ODR` × `M_CUST_ITEM` で得意先品番へ写像）で取得し、集計スナップショットに保持する（出荷推移 V-216 と同じ方針）。内示受注そのものの定義は 5年9組コンテキストの T-504 に従う（**core への昇格候補**。2 コンテキストで同一の意味）。
  **内示受注は得意先 × 内作品番の単位のため、同じ内作品番を共有する得意先品番の行には同じ値が現れる**（入荷推移 V-217 と同じ重複計上の性質。実例: 得意先 137 の内示受注が 96160-00500 と 10523-X0A02 の両方に現れる）。
  実データ（2026-09-15）では内示受注は翌月から 3 か月分まで存在し、一覧 2,298 行中 1,354 行（59%）に内示受注がある。通常流動品では 92%、低流動品（入荷なし）では 74% がカバーされる。

### V-220: 需要予測

- **英語名**: `DemandForecast`
- **定義**: 照合単位（T-208）の **月次需要の見込み**。翌月〜翌々々月は内示推移（V-219）を照合単位で合算した値、それ以降はその 3 か月の平均（内示推移が 3 か月に満たない得意先しかない場合は、**存在する月のみで平均**する）。**内示推移が 3 か月とも 0 なら「なし」**。出荷実績による代替（旧「実績ベース」）は **行わない**（2026-09-18 改訂。内示と実績平均が混在すると数値の意味が読み手に伝わらないため、予測は内示のみに基づく）。
- **構成要素**: 月次数量（バラ数）と算出根拠（`内示` / `なし`）。
  > **［2026-09-18 改訂前］** 内示推移が 3 か月とも 0 で直近 12 か月（同日の一時改訂案では 3 か月）に出荷実績があれば月平均出荷数量で代替し、算出根拠 `実績ベース` と明示していた。
- **使用しない表現**: 「需要見込み」「出荷予測」「予測出荷」（core T-003 予測込み合計と混同するため）「フォーキャスト」
- **補足**: 合算では **(得意先, 内作品番) の組で重複を除く**（V-219 の重複計上を打ち消すため）。**算出は SLIMS 取込時にサーバ側（domain）で行い、結果を集計スナップショットに保持する**（2026-09-16 設計承認で「クライアント側」から改訂。CSV 出力と画面で同じ値を使うため）。一覧表示のたびの新規 Oracle 問い合わせは発生しない。

### V-221: 在庫切れ予測月

- **英語名**: `StockoutForecastMonth`
- **定義**: 照合単位の在庫数（SLIMS 在庫数 V-206 の合計）から、当月残の内示受注数量と累積の需要予測（V-220）を差し引いて **初めて 0 を下回る年月**。需要予測が「なし」なら空。在庫が 0 でも需要が出るまでは尽きないので、**在庫 0 の照合単位は需要（当月残または月別需要）が初めて出る月**になる（2026-09-21 改訂。従来の「在庫 0 以下は当月」を廃止）。
- **構成要素**: 年月（`YYYY-MM`）または空。
- **使用しない表現**: 「欠品予測」「在庫枯渇日」「切れ月」「欠品月」
- **補足**: 低流動品（入荷なし）の優先順位付けと、在庫切れリスク（S-204）の猶予日数（V-228）に用いる。推定在庫推移（V-218）が過去向きの逆算であるのに対し、本項目は **未来向きの見込み**。在庫数が未取得（`－`）の照合単位では算出しない。SLIMS に行がない（該当なし）は在庫 0 として計算する（流動区分 S-203 では 欠品／打ち切り候補 の判定材料）。

### V-222: 在庫月数

- **英語名**: `MonthsOfStock`
- **定義**: 照合単位の在庫数 ÷ **翌月〜翌々々月の需要予測（V-220）の平均**。「あと何か月もつか」の目安。需要予測が「なし」または 0 なら空。
- **構成要素**: 月数（小数 1 桁）または空。
- **使用しない表現**: 「在庫回転月数」「在庫日数」「カバー月数」
- **補足**: 一覧のソートキーとして用いる（昇順＝緊急）。在庫切れ予測月（V-221）と対で表示する。

### V-223: 緊急度

- **英語名**: `UrgencyIndicator`
- **定義**: 低流動品（入荷なし）に対して、仕入先確認の優先順位を示す指標。**在庫月数（V-222）と在庫切れ予測月（V-221）の組**で、需要予測（V-220）の算出根拠（`内示`）を伴う。需要予測が「なし」なら緊急度も「なし」。
- **構成要素**: 在庫月数（小数 1 桁または空）、在庫切れ予測月（`YYYY-MM` または空）、算出根拠。
- **使用しない表現**: 「優先度」「危険度」「切迫度」「アラートレベル」（S-202 と混同するため）
- **補足**: 詳細ダイアログに表示する（2026-09-17 改訂: 一覧のセルは区分名のみ）。判定（S-203）には用いない。在庫切れリスク（S-204）の判定では、本項目の在庫切れ予測月から猶予日数（V-228）を導く。

### V-224: 発注残 【草案・承認待ち 2026-09-17】

- **英語名**: `OpenPurchaseOrder`
- **定義**: 基幹 Oracle（MARI）の発注データ（`T_RLSD_PUCH_ODR`）のうち、**発注済みで未検収の数量が残っている明細**。発注数量から検収済み数量（`T_PAST_INSPC_ACPT`）を引いた **残数** と **納期** を持つ。
- **構成要素**: 仕入先品番（`ITEM_CD`）、仕入先（`VEND_CD`）、納期（`PUCH_ODR_DLV_DATE`。回答納期 `CONFIRM_DLV_DATE` の扱いは要件で確定）、残数（発注数量 − 検収数量 > 0）。対象は状態 `PUCH_ODR_STS_TYP = '2'` かつ取消（`ODR_CANCEL_SLIP_ISS_FLG`）なし。5年9組の残数算出と同じ条件。
- **使用しない表現**: 「未納」「発注中」「入荷予定」（単独）「オープン PO」
- **補足**: 突合は **仕入先品番 × 仕入先**。2026-09-18 から完成品直下の工程（`level1_item_cd` × `level1_vend_cd`）だけでなく、BOM を末端まで辿った **工程の連鎖（外注工程の列）** の全工程で集め、直下の工程の分を補充見込み（V-226）、上流工程の分を「上流工程の発注残」として区別する。納期を過ぎた発注残は **納期遅れで入荷待ち**のものとして扱い、取得からは除外しない（2026-09-17 業務回答: 閉じ忘れではない）。ただし、超過が リードタイム（V-225）＋安全日数 を超えるものは **長期納期超過（V-230）** として補充見込み（V-226）には数えない（2026-09-21 改訂）。実データ（2026-09-17）: 3,310 明細、うち納期超過 1,980（1,520 は 3 か月以上前）。

### V-225: リードタイム 【草案・承認待ち 2026-09-17】

- **英語名**: `LeadTime`
- **定義**: 発注してから入荷するまでに要する **日数**。基幹の品目マスタ `M_ITEM.FIXED_LT` の値（単位: 日。2026-09-17 業務回答）。空または 0 の品番は既定値（設定画面で変更可）で代替する。
- **構成要素**: 日数（整数）。
- **使用しない表現**: 「LT」（文書中の単独表記）「納入リードタイム」「調達期間」「発注サイクル」
- **補足**: 実データでは 19,432 品番中 17,606 に値があり、2〜5 日が大半。在庫切れリスク（S-204）で「今発注しても間に合うか」の境界に用いる。判定には **工程の連鎖の合計**（各外注工程のリードタイムの和）を用いる（2026-09-18）。`ODR_LT` / `PROP_LT` / `SAFETY_LT` はほぼ未使用のため用いない。

### V-226: 補充見込み 【草案・承認待ち 2026-09-17】

- **英語名**: `ReplenishmentOutlook`
- **定義**: **補充期限（V-229）までに納期がある** 発注残（V-224）のうち、長期納期超過（V-230）でないものの **残数の合計**。照合単位（T-208）に属する完成品直下の工程の仕入先品番 × 仕入先の発注残を合算する（2026-09-21 改訂。従来は在庫切れ予測月の月末まで・納期超過は全て含めていた）。
- **構成要素**: 数量（バラ数）、納期超過の有無（長期納期超過を含む）、最も早い納期、補充期限より後の残数（参考）、長期納期超過の残数（参考）。
- **使用しない表現**: 「入荷予定数」「発注済数」「手配済み」
- **補足**: 不足数量（在庫切れ予測月までの需要合計 − 在庫合計）と比べて、補充が需要をカバーするかを判定する。0 なら「補充の見込みなし」。長期納期超過の発注残は「来るかどうか分からない」ものとして補充には数えず、理由「納期遅れ」で納期確認を促す。

### V-227: 発注方式 【草案・承認待ち 2026-09-17】

- **英語名**: `OrderingMethod`
- **定義**: 品番ごとの発注の出し方。基幹の品目マスタ `M_ITEM.MRP_ODR_TYP` で、**`4` = 手動発注**（担当者が発注を起こす）、**`5` = MRP 発注**（所要量計算で自動起票）。その他の値は「不明」として扱う（2026-09-17 業務回答）。
- **構成要素**: 手動発注 / MRP 発注 / 不明 の 3 値。
- **使用しない表現**: 「発注区分」「手発注」「自動発注」「MRP 品」（単独）
- **補足**: 手動発注の品番は **発注忘れ**が在庫切れの主要因になるため、在庫切れリスク（S-204）の理由表示に用いる。実データでは手動発注 565 品番、MRP 発注 18,860 品番。

### V-228: 猶予日数 【草案・承認待ち 2026-09-17】

- **英語名**: `DaysUntilStockout`
- **定義**: 基準日（BOM 基準日、取込暦日）から **在庫切れ予測月（V-221）の 1 日** までの日数。在庫切れ予測月が当月なら 0。在庫切れ予測月が空（需要なし・120 か月超）なら定義しない。
- **構成要素**: 日数（整数、0 以上）。
- **使用しない表現**: 「残日数」「在庫日数」（在庫月数 V-222 と混同するため）「余裕」
- **補足**: 在庫切れ予測が月単位のため月初基準で保守的に取る。リードタイム（V-225）＋安全日数（設定）と比べて「今発注しても間に合うか」を判定する。補充側の期限は補充期限（V-229）で対にする。

### V-229: 補充期限 【草案・承認待ち 2026-09-21】

- **英語名**: `ReplenishmentDeadline`
- **定義**: 発注残（V-224）が **この日までに入荷すれば在庫切れを避けられる** とみなす期限。**max(在庫切れ予測月（V-221）の 1 日, 基準日) ＋ 安全日数（設定）**。在庫切れ予測月が当月なら 基準日 ＋ 安全日数。
- **構成要素**: 日付。
- **使用しない表現**: 「入荷期限」「納入期限」「デッドライン」
- **補足**: 猶予日数（V-228）が在庫切れ予測月の 1 日を基準に保守的に取るのと対で、補充側にも安全日数を効かせる（2026-09-21 追加。従来は予測月の月末までを「間に合う」としていたため、月初に尽きる品目でも月末納期の発注残で対象外になっていた）。上流工程の発注残が「流れている」とみなす納期の窓にも用いる。

### V-230: 長期納期超過 【草案・承認待ち 2026-09-21】

- **英語名**: `StaleOverdue`
- **定義**: 納期超過の発注残（V-224）のうち、**納期からの超過日数が リードタイム（V-225）＋安全日数（設定）を超える** もの。
- **構成要素**: 残数（バラ数）。
- **使用しない表現**: 「閉じ忘れ」（業務回答で否定されている）「不良債権」「デッド PO」
- **補足**: 業務回答「閉じ忘れではない」は発注が有効かの回答であって、間に合うかの回答ではない。補充見込み（V-226）には数えず、理由「納期遅れ」で仕入先への納期確認を促す（2026-09-21 追加）。実データ（2026-09-17）では納期超過 1,980 明細中 1,520 が 3 か月以上前で、ほぼ全てが該当する見込み。

---

## 3. ステータス

### S-201: 確認状態

- **英語名**: `ConfirmationStatus`
- **定義**: 確認記録（E-201）が取り得る状態。**未確認** / **確認中** / **確認済み** の 3 値。
- **構成要素**: `unconfirmed`（未確認）/ `in_progress`（確認中）/ `confirmed`（確認済み）
- **使用しない表現**: **「確認済」（送り仮名なし）**「対応済み」「チェック済み」「処置済み」
- **適用範囲**: **表示ラベルとして用いる場合に限る。** 行の背景色を決めるための内部トークン（`ROW_CLASS_CONFIRMED` = `確認済`。API 応答の `alertRowClass`）は対象外とする。画面に表示する語は必ず「確認済み」とする。
- **補足**: 昇順は 未確認 → 確認中 → 確認済み。ソートは表示ラベルではなくキーで行う。

### S-202: アラートレベル

- **英語名**: `AlertLevel`
- **定義**: 入荷と出荷のバランスから導く注意度。**重点** / **警告（出荷あり）** / **警告（出荷なし）** / **アラート無し** の 4 値。
- **使用しない表現**: **「アラートなし」「問題なし」「警告（出荷）」「警告（入荷）」**（いずれも旧ラベル。読込時に正規化して扱う）
- **補足**: 判定優先順は 重点 → 警告（出荷あり） → 警告（出荷なし） → アラート無し。在庫数は判定に用いない。
- **改定（2026-08-27）**: **流動区分（S-203）に置き換える。** 新規の画面・API・文書では S-203 を用い、アラートレベルは **確認記録（E-201）に保存済みの「確認時のアラートレベル」を読み込むための互換用としてのみ残す**。旧ラベルと流動区分の対応、および確認記録の移行方法は設計フェーズで確定する。

### S-203: 流動区分

- **英語名**: `FlowQuadrant`
- **定義**: 行（得意先 × 得意先品番）を、**SLIMS 在庫の有無・需要の有無・直近の入荷** と **判定期間（V-211）内の期間内入荷（V-212）／期間内出荷（V-213）の有無** から導く 7 分類（2026-09-18 改訂）。**欠品（入荷なし）** / **欠品** / **低流動品（入荷なし）** / **在庫死蔵品** / **低流動品（出荷なし）** / **打ち切り候補** / **通常流動品**。
- **構成要素**（判定は上から順に評価し、最初に該当した区分にする）:

  | ランク | 流動区分 | 判定条件 | 状況 | 推奨アクション（T-207・暫定） | 責任部署（R-201） |
  |:---:|---|---|---|---|---|
  | 0 | 欠品（入荷なし） | SLIMS 在庫なし かつ 需要あり かつ {recent_days} 日以内に入荷なし | 在庫なし・需要あり、最終入荷 {last_incoming}（直近 {recent_days} 日入荷なし） | 発注状況・仕入先の納期を至急確認。発注がなければ即発注 | 調達G / 生産管理 |
  | 1 | 欠品 | SLIMS 在庫なし かつ 需要あり かつ {recent_days} 日以内に入荷あり | 在庫なし・入荷はあるが在庫が残らない（最終入荷 {last_incoming}・最終出荷 {last_ship}） | 供給遅延。工程の連鎖で止まっている箇所を確認し納期前倒しを依頼 | 生産管理 / 調達G |
  | 2 | 低流動品（入荷なし） | 在庫あり かつ 期間内出荷あり かつ 期間内入荷なし | 出荷は続くが判定期間以上入荷がない（在庫を食いつぶしている）。最終入荷 {last_incoming} | 仕入先へ生産継続可否・設備/金型の有無を確認。在庫切れ予測月（V-221）が近いものから | 調達G / 営業G / 生産管理 |
  | 3 | 在庫死蔵品 | 在庫あり かつ 期間内入荷なし かつ 期間内出荷なし | 判定期間以上、入荷も出荷もない | 処分・廃却の検討、得意先へ補給要否を確認 | 調達G |
  | 4 | 低流動品（出荷なし） | 在庫あり かつ 期間内入荷あり かつ 期間内出荷なし | 入荷はあるが判定期間以上出荷がない。最終出荷 {last_ship} | 発注を抑制、営業へ需要を確認。内示があれば立ち上がり／出荷経路を確認 | 営業G |
  | 5 | 打ち切り候補 | SLIMS 在庫なし かつ 需要なし | 在庫なし・内示なし（最終出荷 {last_ship}） | 営業へ終了を確認。終了なら確認済みにする | 営業G |
  | 6 | 通常流動品 | 上記以外（在庫あり・期間内に入荷も出荷もあり） | 入荷・出荷とも動いている | なし（一覧のセルは空） | 生産管理 |

  - **在庫あり／なし**: SLIMS 在庫（V-206）に当該得意先品番の行が **ある／ない**（SLIMS は残数 0 のロットが消えるため「ない」は在庫 0 と同義）。在庫が未取得の旧スナップショットは「あり」として扱う
  - **需要あり**: 内示推移（V-219）に数量がある（＝需要予測 V-220 の算出根拠が `内示`）。出荷実績は需要の判定に **用いない**（2026-09-18 改訂）
  - **30 日**（直近入荷の窓）は設定値（既定）。判定期間（1/3/5 年）は期間内入荷・期間内出荷にのみ効く。
    上表の状況・判定条件では、これを **`{recent_days}`（日数）** のプレースホルダで表す（`{period}` は判定期間、`{last_incoming}` / `{last_ship}` は日付）
  - 欠品 2 区分と打ち切り候補は判定期間によらない（判定期間を切り替えても変わらない）

  **理由（2026-09-18 新設、2026-09-21 改訂）**: 区分をそう判定した根拠のうち、区分名だけでは伝わらないものを短い文言で添える。詳細ダイアログの流動区分セクションに箇条書きで示す（一覧のセルには出さない）。該当がなければ空。

  | 理由 | 対象区分 | 条件 |
  |---|---|---|
  | 入荷の記録なし・経路要確認 | 欠品（入荷なし） | 最終入荷日（V-215）が空（入荷実績が一度もない） |
  | {判定期間}以上出荷なし・経路要確認 | 欠品・欠品（入荷なし） | 期間内出荷（V-213）がない（最終出荷日が空も含む）。入荷は来ているのに出荷が基幹に記録されない経路の疑い |
  | 入荷 < 需要 | 欠品 | 翌月の内示 > 0 かつ、最終入荷日が属する月の入荷推移（V-217）の合計がそれを下回る |
  | 内示あり（立ち上がり／出荷経路要確認） | 低流動品（出荷なし） | 需要あり（需要予測 V-220 の算出根拠が `内示`） |
  | 適用終了日 YYYY/MM/DD | 打ち切り候補 | 得意先品目マスタの適用終了日が基準日より前 |

  - **使用しない表現**: 「警告」「コメント」「注記」（理由に統一）
  - 理由のうち 2 つ（{判定期間}以上出荷なし・内示あり）は判定期間で変わるため、行は **3 期間ぶん（Y1/Y3/Y5）** の理由を持ち、一覧の判定期間切替ではクライアントが引き直す
    > **［2026-09-21 改訂前］** 2 行目は「出荷の記録なし・経路要確認」（最終出荷日が空）だった。一覧は出荷実績のある 得意先 × 得意先品番 から組み立てるため該当が構造上存在せず（実データ 2,299 行中 0 件）、理由が一度も出なかった。期間判断の基準を判定期間に一本化した際に条件を「期間内出荷なし」へ改め、対象を欠品 2 区分に広げた

- **使用しない表現**: **「供給リスク品」「在庫過剰リスク品」**（2026-09-15 までの旧称。低流動品（入荷なし）／低流動品（出荷なし）に改称）、**「象限1」「象限2」「象限3」「象限4」**（番号のみでの参照）、「4象限区分」「流動性区分」「デッドストック区分」、「低流動品（出荷あり）」「低流動品（入荷あり）」（内訳は **欠けている側** で表す）
- **ランク（深刻度・並び順）**: 上表のとおり **欠品（入荷なし）= 0 → 欠品 = 1 → 低流動品（入荷なし）= 2 → 在庫死蔵品 = 3 → 低流動品（出荷なし）= 4 → 打ち切り候補 = 5 → 通常流動品 = 6**。数値が小さいほど深刻。一覧の並び順（在庫切れリスク S-204 の次）と深刻化による未確認化（A-201）に用いる。
- **補足**: 判定単位は **得意先 × 得意先品番** の 1 行。2026-09-18 から **SLIMS 在庫の有無と需要の有無を判定に用いる**（在庫がない行を欠品 2 区分と打ち切り候補に振り分けるため）。一覧のセルには区分名のみを表示し、状況・推奨アクション・責任部署は詳細ダイアログに示す。
  > **［2026-09-18 改訂前］** 期間内入荷 × 期間内出荷の 4 分類（在庫死蔵品／低流動品（入荷なし）／低流動品（出荷なし）／通常流動品）で、在庫の有無は判定に用いていなかった。SLIMS 在庫なしの行が「在庫 0」として在庫切れリスク（S-204）の危険に大量に上がり、供給遅延・打ち切り・本当の欠品を切り分けられなかったため 7 分類に改めた（spec/07_flow-quadrant-refinement）。
  > **［2026-09-15 改訂前］** 供給リスク品（入荷なし・出荷あり）／在庫過剰リスク品（入荷あり・出荷なし）と呼び、判定軸（V-210）によって「在庫死蔵品」の呼称を使い分けていた。

---

### S-204: 在庫切れリスク 【草案・承認待ち 2026-09-17】

- **英語名**: `StockoutRisk`
- **定義**: 「お客さんの納期どおりに納める（在庫切れを起こさない）」という目的に対して、行ごとの対応の優先順位を示す 4 段階。在庫切れ予測月（V-221）・猶予日数（V-228）・補充見込み（V-226）・リードタイム（V-225）から判定する。
- **構成要素**:

  | 在庫切れリスク | 条件（要件定義書で確定） | 意味 |
  |---|---|---|
  | **危険** | 猶予日数 ≤ リードタイム ＋ 安全日数 かつ 補充見込みが 0 または不足数量に満たない かつ 免除なし（免除 = MRP 発注で補充サイクルが動いている／上流工程に補充期限内の発注残がある） | 今発注しても間に合わない見込み。即時の手配・仕入先確認 |
  | **注意** | 在庫切れ予測が監視期間内で、補充見込みが 0（まだ猶予あり）／不足数量に満たない／納期超過の発注残がある | 発注・納期の確認を促す |
  | **監視** | 需要なし、または在庫切れ予測が監視期間より先 | 急がない。定期的に見直す |
  | **対象外** | 補充見込みが不足数量をカバーし納期超過もない。または 通常流動品の MRP 発注で発注期限（在庫切れ予測 − リードタイム − 安全日数）がまだ先で発注残がない（所要量計算が起票する前の正常状態） | 通常の JIT。目立たせない |

  条件の確定版は要件定義書 F-005（2026-09-21 改訂）。

- **ランク（並び順）**: 危険 = 0 → 注意 = 1 → 監視 = 2 → 対象外 = 3。同順位は猶予日数の昇順。
- **使用しない表現**: 「アラートレベル」（S-202 の旧称と混同）「危険度」「優先度」「欠品リスク」（在庫切れに統一）「ランク」（単独では S-203 のランクと紛れる）
- **補足**: 流動区分（S-203）は「なぜ補充が来ないか」の理由と責任部署（R-201）を示す軸として併用する。判定はデータ更新（取込）時に行い、スナップショットに保存する。

## 4. 専門用語

### T-201: 最終入荷日以降の出荷

- **英語名**: `PostIncomingShipment`
- **定義**: `T_SHIP` において `DEL_FLG != 1` かつ `SHIP_DATE` が最終入荷日（V-201）より後である明細。最終入荷日が空の場合はすべての出荷を指す。
- **使用しない表現**: 「入荷後出荷」「入荷なし出荷」
- **補足**: 出荷回数・出荷数合計の集計対象。検証スクリプトの `aggregate_shipment_stats` と同一定義。

### T-202: 在庫時点

- **英語名**: `StockAsOf`
- **定義**: SLIMS 在庫 CSV を最後に取り込んだ暦日。画面には「YYYY年M月D日時点の在庫」形式で表示する。
- **使用しない表現**: 「在庫基準日」「棚卸日」

### T-203: 警告条件

- **英語名**: `AlertRules`
- **定義**: 入荷と出荷のバランスからアラートレベル（S-202）を決める **判定ルール** の総体。一覧の「警告条件」ダイアログで内容を確認し、月数を変更できる。
- **構成要素**: 出荷ありの判定月数（`warningShipmentMonths`）と出荷なしの判定月数（`warningIncomingMonths`）。いずれも **1〜36 か月**、既定 **12**。
- **使用しない表現**: 「警告閾値」「アラート条件」「判定基準」
- **補足**: 旧方式の `warningDays`（既定 365 日）は **非推奨** であり、DB に保持するのみで判定には用いない。月数の変更はデータ更新（core A-003）を要さず、表示時に再判定する。
- **改定（2026-08-27）**: アラートレベル（S-202）を流動区分（S-203）へ置き換えるのに伴い、`warningShipmentMonths` / `warningIncomingMonths` も **`warningDays` と同じ扱い**（DB に保持するのみで判定には用いない）とする。流動区分は判定期間（V-211）の固定 6 値で判定する。設定画面の扱いは設計フェーズで確定する。

### T-204: 4象限分析

- **英語名**: `QuadrantAnalysis`
- **定義**: 判定期間（V-211）内の入荷と出荷の有無で品目を 4 つに分け、**状況・推奨アクション（T-207）・責任部署（R-201）** を対応づける枠組み。流動区分（S-203）を導く考え方そのものを指す。
- **使用しない表現**: 「マトリクス分析」「ABC分析」「四象限」「2軸分析」
- **補足**: 判定軸（V-210、廃止）の時代は軸ごとに 4 分類していたが、2026-09-15 以降は判定期間（年数）1 つで分類する。

### T-205: 低流動品

- **英語名**: `LowFlowItem`
- **定義**: **SLIMS 在庫があり**、判定期間（V-211）内に **入荷・出荷のどちらか一方だけ**がある品目。欠けている側で **低流動品（入荷なし）** と **低流動品（出荷なし）** に分ける（S-203）。在庫がない品目は欠品（T-209）または打ち切り候補（T-210）になる（2026-09-18）。
- **使用しない表現**: 「滞留品」「不動品」「動かない品番」「低回転品」「供給リスク品」「在庫過剰リスク品」（旧称）
- **補足**: 「流動が低い」＝「片方が止まっている」と定義するため、月単位の短期の窓は持たない。
  > **［2026-09-15 改訂前］** 低流動判定軸（1・3・6 か月）で判定した結果、流動区分が通常流動品以外となった品目。

### T-206: 在庫死蔵品

- **英語名**: `DormantStockItem`
- **定義**: **SLIMS 在庫があり**、判定期間（V-211、1・3・5 年）内に **入荷も出荷も発生していない** 品目。流動区分（S-203）の「在庫死蔵品」に該当する。在庫がなく需要もない品目は打ち切り候補（T-210）とする（2026-09-18）。
- **使用しない表現**: **「未流動品」**「デッドストック」「不良在庫」「長期滞留在庫」「休眠在庫」「低流動品（入出荷なし）」
- **補足**: 「未流動品」は元資料のタイトル表記であり、本書および仕様書では **在庫死蔵品** に統一する。判定期間が 1 年でも 5 年でも同じ呼称を用いる。
  > **［2026-09-15 改訂前］** 死蔵判定軸で判定した品目に限り「在庫死蔵品」と呼び、低流動判定軸（1・3・6 か月）で入出荷なしの品目は「低流動品（入出荷なし）」と呼ぶ規則だった。判定軸の廃止に伴いこの使い分けは削除した。

### T-209: 欠品

- **英語名**: `StockoutItem`
- **定義**: **SLIMS 在庫がなく、需要がある**品目（2026-09-18 新設）。直近入荷の窓（設定 `recentIncomingDays`・既定 30 日・範囲 1〜90。2026-09-21 に設定項目化）内の入荷の有無で **欠品（入荷なし）**（補充が来ていない）と **欠品**（入荷はあるが在庫が残らない＝供給が遅れ、入荷したそばから出荷している）の 2 区分に分ける（S-203）。区分名としての「欠品」は後者を指し、本用語（T-209）は 2 区分の総称。
- **使用しない表現**: 「品切れ」「在庫切れ品」（V-221 在庫切れ予測月と紛れる）「通過品」「自転車操業品」（欠品の俗称。正常な運用ではなく遅延の症状であるため区分名にしない）**「欠品」**（2026-09-18 の一時的な区分名。同日に「欠品」へ改名）
- **補足**: 「需要がある」は内示推移（V-219）に数量があること（出荷実績は見ない）。入荷の記録が一度もない欠品（入荷なし）は、基幹で入荷を追えない経路（内製・別倉庫等）の可能性があり、理由「入荷の記録なし・経路要確認」を添える。

### T-210: 打ち切り候補

- **英語名**: `DiscontinuationCandidate`
- **定義**: **SLIMS 在庫がなく、需要もない**（内示がない）品目（2026-09-18 新設）。出荷が続いていても内示がなければ本区分になる。供給が終わった、または終わりつつある可能性がある。
- **使用しない表現**: 「廃番」「終了品」「打ち切り品」（確定情報が基幹にないため「候補」と呼ぶ）「死蔵」（在庫がある T-206 と区別）
- **補足**: 得意先品目マスタ（`M_CUST_ITEM`）の適用終了日が過去なら理由「適用終了日 YYYY/MM/DD」を添える（実データで 7 件）。確認済みにした行は対象外として扱う運用。

### T-207: 推奨アクション

- **英語名**: `RecommendedAction`
- **定義**: 流動区分（S-203）ごとに定める、担当者が取るべき対応の文言。S-203 の構成要素の表を正とする。**文言は暫定**であり、要件定義（Phase 2）で業務側と確定する。
- **使用しない表現**: 「対応指示」「ToDo」「対策」「アクションプラン」
- **補足**: 元資料の第 3 弾「判断支援」に相当する。一覧のセルに表示するのみで、実施状況の記録（第 4 弾「アクション追跡」）は対象外。
  **実装上の束ね方（2026-09-16、設計書 §4.3）**: 推奨アクションは、流動区分ごとの **状況テンプレート**（S-203 の「状況」をプレースホルダ `{period}` / `{last_incoming}` / `{last_ship}` / `{recent_days}` 付きの文字列で表したもの）と責任部署（R-201）と束ねて `RecommendedAction` として扱う。**7 区分ぶん**のコレクションが `RecommendedActions`（欠け・重複を許さない）。
  **2026-09-18 改訂（spec/07_flow-quadrant-refinement）**: S-203 の 7 分類化に伴い 3 区分ぶんを追加し、`{recent_days}`（直近入荷の窓・既定 30 日）のプレースホルダを新設した（同日に新設した `{demand_window}`（需要の窓）は、需要の判定を内示のみに改めたため撤去）。

### T-208: 照合単位

- **英語名**: `ReconciliationUnit`
- **定義**: 得意先品番と「内作品番 × 仕入先」の組を節点とする二部グラフの **連結成分**。在庫（得意先品番）・出荷（得意先 × 得意先品番）・入荷（内作品番 × 仕入先）・内示受注（得意先 × 内作品番）の粒度が異なるため、数量を突き合わせる **最小の閉じた集合**。
- **使用しない表現**: 「品番グループ」「共通品グループ」「集計単位」（単独）
- **補足**: V-218 推定在庫推移の定義文中で既に用いていた語を正式化した（2026-09-15）。V-220〜V-222 も同じ単位で算出する。実データ（2026-09-07 取込）では 1,994 単位のうち複数の得意先品番を含むのは 25 単位（144 行）、最大は 37 品番。

---

## 5. ロール

### R-201: 責任部署

- **英語名**: `ResponsibleDepartment`
- **定義**: 流動区分（S-203）ごとに、推奨アクション（T-207）の **一次責任** を負う社内部署。**調達G** / **営業G** / **生産管理** の 3 つ。対応表は S-203 の構成要素を正とする（欠品（入荷なし）＝調達G / 生産管理、欠品＝生産管理 / 調達G、低流動品（入荷なし）＝調達G / 営業G / 生産管理、在庫死蔵品＝調達G、低流動品（出荷なし）＝営業G、打ち切り候補＝営業G、通常流動品＝生産管理）。**複数部署を挙げる場合は先頭が一次責任**で、以降は連携先の順に並べる。
- **使用しない表現**: 「担当部署」「所管部署」「アクション担当」「主管部門」
- **補足**: 本アプリでは一覧・ダッシュボードへの **表示のみ** に用いる（自動通知・アクション実施状況の記録は次弾）。**責任部署を一意に定める点は業務側で未確定**（元資料 §8 の注記）であり、現時点の割当ては暫定である。担当者コード（V-207）とは無関係の概念。

---

## 6. アクション

### A-201: 深刻化による未確認化

- **英語名**: `EscalationReset`
- **定義**: SLIMS 在庫 CSV 取込（データ更新）後に、行の流動区分（S-203）が確認時よりランクの小さい（より深刻な）区分へ変わった場合、確認状態（S-201）を自動で **未確認** に戻す処理。判定は既定の判定期間（V-211、1 年）で固定して行う。
- **使用しない表現**: 「深刻化」（単独。事象と処理を区別するため）「自動リセット」「確認取消」「エスカレーション」
- **境界コンテキスト**: 在庫発注アラート
- **補足**: 機能仕様書 REQ-F-011（§4.1.7）で既に実装されている挙動を用語として正式化した（2026-09-16）。設定画面の「確認状態リセット」（管理者による手動の全件リセット）とは別の処理。確認メモは維持する。

---

## 改定履歴

| 日付 | 変更内容 | 理由 |
|------|----------|------|
| 2026-09-21 | **V-211 に「期間判断の基準」を明記**（期間にかかわる判断はすべて判定期間で行い、選択 UI のない場所は既定 1 年）。S-203 の理由「出荷の記録なし・経路要確認」を **「{判定期間}以上出荷なし・経路要確認」** に改め、対象を欠品 2 区分に広げる | ユーザー判断「これは固定、これは設定によるなど煩雑なので基準は一つにしたい」。旧条件（最終出荷日が空）は一覧が出荷実績のある行から作られる構造上 該当 0 件で機能していなかった（実データ 2,299 行で確認） |
| 2026-09-21 | **V-221 在庫切れ予測月の「在庫 0 以下は当月」を廃止**し、在庫 0 の照合単位は需要が初めて出る月とする。S-204 の危険は欠品（T-209）でも猶予日数（V-228）で判定する（補充サイクル稼働中の免除と MRP 先送りは在庫ありの行のみ） | 07 実装時の判定レビュー。在庫 0 で 11 月まで需要がない行が「当月切れ・危険」になり、今すぐ手を打つ行と発注状況を確認する行が分かれなかったため（spec/07 requirements F-002/F-004） |
| 2026-09-18 | **S-203 ランク 1 の区分名「欠品（入荷即出荷）」を「欠品」に改名**（キー `stockout-pass-through` → `stockout`。判定条件・状況・推奨アクション・責任部署は不変）。T-209 を追随 | ユーザー判断。「入荷即出荷」は判定の説明であって区分名として長く、欠品（入荷なし）との対比は括弧の有無で足りるため |
| 2026-09-18 | **V-220 需要予測を内示のみに改訂**（出荷実績による「実績ベース」の代替を廃止、算出根拠は `内示` / `なし` の 2 値）。S-203 の「需要あり」を「内示推移に数量がある」のみに改め、需要の窓（3 か月・`{demand_window}`）を撤去。T-209・T-210・T-207・V-218・V-222・V-223 を追随 | ユーザー判断（spec/07_flow-quadrant-refinement）。内示と実績平均が混在すると予測値の意味が読み手に伝わらず混乱するため。内示のない品目は在庫切れリスク（S-204）が「監視」となり人が見る運用とする |
| 2026-09-18 | **S-203 の新 3 区分の状況・推奨アクション・責任部署を design.md §2.4 に合わせて確定**（欠品（入荷なし）＝調達G / 生産管理、欠品（入荷即出荷）＝生産管理 / 調達G）。状況テンプレートに `{recent_days}` / `{demand_window}` を導入し、T-207・R-201 を追随 | 用語集（S-203 表）と後続の design.md・test-design.md（TC-FQR-A-002）で責任部署と文言が食い違っていたため、詳細度の高い design.md を正とした。欠品は「発注」と「工程の連鎖」の双方を見る必要があり、調達G 単独では一次責任が閉じない |
| 2026-09-18 | **S-203 流動区分を 7 分類に改訂**（欠品（入荷なし）／欠品（入荷即出荷）／打ち切り候補を新設、在庫の有無と需要の有無を判定に追加、ランク再定義）。**V-220 需要予測の実績ベースを直近 3 か月平均に変更**。T-205・T-206 を追随 | ユーザーとの対話（spec/07_flow-quadrant-refinement）。SLIMS 在庫なしの行（693/2,299）を「供給遅延で入荷即出荷」「打ち切り」「本当の欠品」に切り分ける必要があり、通過品は正常運用ではなく遅延の症状だという業務知識による |
| 2026-09-17 | 【草案・承認待ち】**V-224 発注残・V-225 リードタイム・V-226 補充見込み・V-227 発注方式・V-228 猶予日数・S-204 在庫切れリスクを新設**。V-223 緊急度の補足を一覧セル簡略化に合わせて更新 | 目的を「お客さんの納期どおりに納める＝在庫切れを起こさない」に置き直し（spec/06_stockout-risk 壁打ち）、在庫切れの要因（発注忘れ・納期遅れ・数量不足・リードタイム・仕入先の廃棄）を判定するための用語。業務回答: `MRP_ODR_TYP` 4 = 手動、`FIXED_LT` は日、納期超過の発注残は閉じ忘れではない |
| 2026-09-16 | T-207 推奨アクションに実装上の束ね方（状況テンプレート・責任部署との `RecommendedAction`）を補足 | 設計書 Design-L1 レビュー指摘 #6 |
| 2026-09-16 | V-220 需要予測の算出場所を「クライアント側」から「取込時にサーバ側（domain）」に改訂 | 設計書 DESIGN-SINGLE-FLOW-VIEW-2026-001 §3.2（CSV 出力との二重実装回避、domain でのテスト固定）の承認による |
| 2026-09-16 | **S-203 にランク（深刻度・並び順）を追加**、**V-223 緊急度・A-201 深刻化による未確認化を新設**、V-219 の起点を「BOM 基準日（取込日）以降」に精緻化、V-220/V-222 の平均の対象月を明確化、V-219〜V-221・T-208 の「内示」単独表記を「内示受注」「内示推移」に修正 | 要件定義書（REQ-SINGLE-FLOW-VIEW-2026-001）の L2 レビュー指摘 #2〜#7。深刻化判定がランクに依存する業務ルールであるため用語集で確定した |
| 2026-09-15 | **V-210 判定軸を廃止**、**V-211 判定期間を 1/3/5 年に一本化**（既定 1 年）、**S-203 流動区分・T-205 低流動品・T-206 在庫死蔵品を「在庫死蔵品／低流動品（入荷なし）／低流動品（出荷なし）／通常流動品」で再定義**（供給リスク品・在庫過剰リスク品を改称）、T-204・R-201 を追随。**V-219 内示推移・V-220 需要予測・V-221 在庫切れ予測月・V-222 在庫月数・T-207 推奨アクション・T-208 照合単位を新設** | 「1 回の検索で死蔵と低流動を見たい」「セルで状況と取るべきアクションが分かるようにしたい」という要望（spec/05_single-flow-view）。目的（仕入先が設備・金型を廃棄する前に気づく）から判定の時間スケールは年単位が適切と判断し、月単位の低流動判定軸は廃止。緊急度は内示受注に基づく需要予測で示す |
| 2026-09-11 | **V-205 在庫内訳・V-201 最終入荷日に SLIMS のロット残数の性質を追記**（入荷日つきの各行は残数であり、出荷のたびに減る。残数0のロットは行ごと消える） | 業務ヒアリングで判明。仕様書に無かったため「入荷日 X のロットが無い＝入荷が SLIMS に未計上」と誤読した経緯がある。入荷実績は MARI で見るべきことを明文化した |
| 2026-09-11 | **V-218 推定在庫推移の算出単位を「照合単位」（内作品番×仕入先の組を共有する得意先品番の集合）に改訂** | 在庫・出荷・入荷の3つの粒度が異なるため、得意先品番単位でも共有がある品番では入荷を過大に引いていた。二部グラフの連結成分まで広げると数量が閉じる（実データでマイナス 240行→210行、96160-00500 は解消）。DECISIONS.md ステージ15参照 |
| 2026-09-11 | **V-218 推定在庫推移の算出単位を「得意先品番」に改訂**（出荷は全得意先分を合算、入荷は内作品番×仕入先の重複を除いて合算）。V-216・V-217 の補足に合算の旨を追記 | 改訂前は出荷が当該得意先分のみ・入荷が内作品番の全量だったため、共通品で入荷が過大に効き構造的にマイナスになっていた（実データで 19.3% の行がマイナス）。起点の在庫数が得意先品番単位であることに出荷・入荷を揃えた。DECISIONS.md ステージ14参照 |
| 2026-09-11 | **V-217 入荷推移の表示を再開**（推定在庫推移グラフ上の棒グラフとして重ね描き）。**V-218 推定在庫推移に 0 起点の規定を追加**（在庫数が「該当なし」の行は 0 を起点に算出、「未取得」の行は算出しない） | ユーザー指示「MARIの入荷の棒グラフを足して」「SLIMSに在庫がないものは0として履歴を作成して」による。入荷は在庫増の実測根拠であり、推定在庫の折れ線と同一軸で対比できると判断根拠になるため。DECISIONS.md ステージ12参照 |
| 2026-09-03 | **V-218 推定在庫推移からMARI起点系列を撤去**（SLIMS起点の1系列のみに変更） | ユーザー指示「MARI視点のグラフは削除して」による。「在庫数(MARI)」欄自体（数値表示）は変更なし。DECISIONS.md参照 |
| 2026-09-03 | **V-216 出荷推移・V-217 入荷推移の定義を改訂**（詳細ダイアログでの独立したグラフ表示を撤去し、「推定在庫推移（V-218）の算出材料として用いる」に変更） | ユーザー指示（「入出荷のグラフはいらない」）により、V-216/V-217専用の折れ線グラフ区分を詳細ダイアログから削除。算出自体は V-218 の入力として維持するため、定義文の「表示」記述のみ実態に合わせて修正。DECISIONS.md参照 |
| 2026-09-03 | **V-218 推定在庫推移 を追加** | 出荷推移・入荷推移から在庫の残量トレンドを視覚的に把握したい要望への対応。現在庫数を起点に逆算する参考値であり実測ではない旨を明記 |
| 2026-09-03 | **V-217 入荷推移 を追加** | 詳細ダイアログの出荷推移グラフ（V-216）に入荷実績も並べて表示する要望への対応。流動区分の判定根拠が入荷・出荷の両方であるため |
| 2026-09-01 | **V-216 出荷推移 を追加** | 詳細ダイアログへ出荷推移グラフを追加する要件（spec/04_shipment-history-chart）の SDD Phase 1。在庫数自体の時系列データが存在しないため、判定根拠となる出荷実績の月次推移を可視化する方針とした |
| 2026-08-31 | **V-206 を「在庫数」から「SLIMS 在庫数」へ改称**し、**V-215 MARI 在庫数 を追加**。あわせて両者に共通品の重複計上の性質を明記 | MARI 在庫を判断材料として併記する要件（REQ-MARI-STOCK-VISIBILITY-2026-001）の L2レビュー 指摘 L2-1・L2-2。出所を明示しない「在庫数」では 2 つの数量を区別できないため |
| 2026-08-27 | V-210 判定軸・V-211 判定期間・V-212 期間内入荷・V-213 期間内出荷・V-214 入荷実績なし・S-203 流動区分・T-204 4象限分析・T-205 低流動品・T-206 在庫死蔵品・R-201 責任部署 を追加。S-202 アラートレベルと T-203 警告条件に置き換えの注記を追加し、ロール節（5.）を新設 | 「低流動品・未流動品の状況把握機能」の追加にあたり、SDD Phase 1 として用語を確定したため。アラートレベルは流動区分へ置き換える方針をユーザー承認済み |
| 2026-07-30 | V-206 在庫数・V-207 担当者コード・V-208 仕入先品番・V-209 BOM 基準日・T-203 警告条件 を追加。S-201 に適用範囲（内部トークンは対象外）を追記。V-205 の分類根拠を明記 | L2レビュー 指摘 L2-3・L2-5・L2-8・L2-12・L2-14。仕様書で頻出する用語が未定義であったこと、および `alertRowClass` の内部トークンが S-201 の禁止語と衝突して見えたため |
| 2026-07-30 | 初版作成（E-201〜E-202、V-201〜V-205、S-201〜S-202、T-201〜T-202） | 機能仕様書のL1レビューで、コンテキスト固有言語集の不在および「確認済／確認済み」「仕入先／仕入れ先」の表記揺れが検出されたため（[tasks.md](../../../docs/spec/l1-review-remediation/tasks.md) 指摘 3-12・3-17・3-19） |
