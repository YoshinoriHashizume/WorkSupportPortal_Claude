# 判断ログ: 出荷推移グラフ（自律実行分）

作成日: 2026/09/01
対応: [requirements.md](./requirements.md) / [design.md](./design.md) / [test-design.md](./test-design.md) / [tasks.md](./tasks.md)

ユーザーから「今日は帰るので、要件定義から実装まで一気にやってみて。確認点は自分で保管して明日確認できるように」との指示を受け、
対話による承認を都度得ずに SDD の各フェーズを自己判断で進めた。**明日、以下を上から順に確認してください。**
いずれも「間違っている」というより「複数の妥当な選択肢のうち1つを選んだ」判断であり、変更が必要ならすぐ直せる想定です。

## 実行サマリー

- **ブランチ**: `feature/ioa-shipment-history-chart`（`develop` から分岐し、実装完了後に `develop` へマージ済み。**`origin` への push は未実施**）
- **成果物**: requirements.md / design.md / test-design.md / tasks.md（本ディレクトリ）、実装コード一式（下記）
- **テスト**: `pytest` リポジトリ全体 1656件 Green（うち本機能の新規テスト約40件）。`manage.py check` 問題なし。マイグレーション不要（`makemigrations --check` で差分なし）
- **変更ファイル**（新規）: `domain/value_objects/shipment_trend.py`、`tests/test_shipment_trend_vo.py`、`tests/test_shipment_trend_query.py`、`tests/test_shipment_trend_edge_cases.py`
- **変更ファイル**（既存改修）: `infrastructure/oracle/summary_queries.py`、`templates/inventory_order_alert/list.html`、`static/js/inventory-order-alert-list-client.js`、`static/js/inventory-order-alert-list.js`、`static/css/app.css`、`docs/在庫発注アラート_機能仕様書.md`、`docs/ubiquitous_language.md`、および関連テストファイル数点
- **Oracle への影響**: **なし**。新規クエリを追加せず、既存の `fetch_all_shipments()` の取得結果を月次に束ね直しただけ

### 追記（2026/09/03、ユーザー指示「入荷も含めて」への対応）

- **ブランチ**: `feature/ioa-shipment-history-chart-incoming`（`develop` から分岐。tasks.md ステージ6 タスク22〜33 実施）
- **テスト**: `pytest` リポジトリ全体 1667件 Green（新規テスト11件追加）。`manage.py check` 問題なし。マイグレーション不要
- **変更ファイル**（新規）: `tests/test_incoming_trend_query.py`
- **変更ファイル**（既存改修）: `infrastructure/oracle/summary_queries.py`（`fetch_incoming_receipts()` 新設、`build_summary_rows()` に `incoming_trend` 付与）、`static/js/inventory-order-alert-list-client.js`（`getIncomingTrend` 追加）、`static/js/inventory-order-alert-list.js`（`renderShipmentTrendChart` を2系列描画に拡張）、`templates/inventory_order_alert/list.html`（見出しを「入出荷推移」に変更）、`static/css/app.css`（入荷系列・凡例のスタイル追加）、`docs/在庫発注アラート_機能仕様書.md`（§4.1.6・改訂履歴4.9）、`docs/ubiquitous_language.md`（V-217追加）
- **Oracle への影響**: **あり（新規クエリ1件）**。`fetch_incoming_receipts()` を取込ごとに1回、直近24か月に絞って発行（詳細は下記「項目3」参照）
- **push**: 本追記分も develop へのローカルコミットのみ。**origin への push は未実施**（本セッションの一貫方針どおり、明示指示待ち）

### 追記（2026/09/03、ユーザー指示「累積でグラフを見たい」への対応）

自律実行分（項目1〜5）とは異なり、本追記はユーザーと対話しながら実施した。負値の表示方針（マイナスのままか0でクランプするか）は AskUserQuestion で確認済み（「マイナスのまま表示する」を選択）。それ以外の実装判断は下記のとおり。

- **算出方式**: 現在の在庫数（SLIMS/MARI）を起点に、入出荷推移から「当月末推定 = 翌月末推定 + 翌月出荷 − 翌月入荷」で過去へ逆算する**参考値**（V-218 推定在庫推移）。実測の在庫履歴ではない旨をユビキタス言語集・要件定義書・機能仕様書に明記した。
- **算出場所の判断**: サーバー側（domain層）ではなく**クライアント（JS）側**で算出する設計にした。出荷推移・入荷推移・SLIMS/MARI在庫数はいずれも既にクライアントへ配信済みのデータであり、既存の `sortValue` 等の派生値計算がクライアント側で行われている既存パターンとも整合するため。この判断により、**新規 Oracle 問い合わせ・配信ペイロード増加ゼロ**で実現できた。
- **色の割当て（未確認）**: SLIMS起点=インディゴ系、MARI起点=緑系とした（出荷=青・入荷=橙と混同しないよう別系統の色にした）。デザイン上の好みで変更可能。
- **数値計算の検証方法**: JS用のテストランナーがないため、自動テストはソース文字列アサーションにとどまる。数値計算の正しさは Node で一時スクリプトを実行し手動検証した（tasks.md ステージ7実行レポート参照）。恒久的な自動テストとしては残っていない点は今後の課題として認識しておく。
- **ブランチ**: `develop` 上で直接実施（ステージ7、tasks.md タスク34〜42）。`origin` への push は未実施（明示指示待ち）
- **テスト**: `pytest` リポジトリ全体 1673件 Green。`manage.py check` 問題なし。マイグレーション不要
- **変更ファイル**（既存改修）: `static/js/inventory-order-alert-list.js`（`parseAnchorQty`・`buildAnchoredStockTrend`・`renderAnchoredStockChart` を新設）、`templates/inventory_order_alert/list.html`（「推定在庫推移（参考値）」区分を追加）、`static/css/app.css`（新系列・ゼロ基準線のスタイル追加）、`docs/在庫発注アラート_機能仕様書.md`（§4.1.6・改訂履歴4.10）、`docs/ubiquitous_language.md`（V-218追加）、`tests/test_inventory_order_alert_list_js.py`（新規テスト6件）

### 追記（2026/09/03、ユーザー指示「入出荷のグラフはいらない」への対応・ステージ8）

ユーザーから「左に台数と入出荷のグラフはいらない」との指示を受けた。曖昧だったため AskUserQuestion で
「詳細ダイアログの『入出荷推移』区分（出荷=青・入荷=橙の2系列グラフ）を区分ごと削除する、という理解であっていますか？」
と確認し、「はい、入出荷推移区分を丸ごと削除」の回答を得てから着手した。

- **撤去した範囲の判断**: 出荷推移（V-216）・入荷推移（V-217）の**算出処理**（Oracle集計・`shipment_trend`/`incoming_trend`の付与）は撤去せず維持した。これらは推定在庫推移（V-218、直前の追記で実装済み）の算出材料として必須のため。撤去したのは**専用グラフとして描画・表示していた部分のみ**（`renderShipmentTrendChart()` 関数本体、`ioa-detail-shipment-trend-section` テンプレート区分、対応CSS）。
- **命名債務の解消**: 直前の追記（項目3）で「許容した」としていた命名債務のうち、CSS/JSクラス名（`.ioa-shipment-trend-svg` 等）は `renderAnchoredStockChart()` が流用していたため、削除ではなく `.ioa-anchored-stock-trend-*` へ付け替えた。一方 `build_monthly_shipment_trend()` / `group_shipments_by_pair()`（Python側、出荷・入荷共通で使う集計関数）は表示撤去と無関係のため変更していない（引き続き design.md R-6 の債務として残る）。
- **仕様書の記述方針**: CLAUDE.mdの「仕様が正しい。コードを修正すること」「削除対象を明示してから実行する」の原則に従い、requirements.md / design.md / test-design.md / 機能仕様書 / ubiquitous_language.md のいずれも元の記述を削除せず、「［撤去済み］」「（削除）」等の注記を付けて履歴を残した。
- **ブランチ**: `develop` 上で直接実施（ステージ8、tasks.md タスク43〜49）。`origin` への push は未実施（明示指示待ち）
- **テスト**: `pytest` アプリ内 656件・リポジトリ全体 1671件 Green。`manage.py check` 問題なし。マイグレーション不要
- **変更ファイル**（既存改修）: `templates/inventory_order_alert/list.html`（「入出荷推移」区分を削除）、`static/js/inventory-order-alert-list.js`（`renderShipmentTrendChart()` 削除、`renderAnchoredStockChart()` のクラス参照を付け替え）、`static/css/app.css`（`.ioa-shipment-trend-*` 削除、`.ioa-anchored-stock-trend-*` 追加）、`docs/在庫発注アラート_機能仕様書.md`（§4.1.6・改訂履歴4.11）、`docs/ubiquitous_language.md`（V-216/V-217定義改訂）、`tests/test_inventory_order_alert_views.py`・`tests/test_inventory_order_alert_list_js.py`（撤去確認テストへ置き換え）

### 追記（2026/09/03、実画面フィードバック3件への対応・ステージ9）

推定在庫推移グラフを実際にブラウザで確認したユーザーから3件の指摘を受けた。事前に変更対象ファイル・影響範囲を提示し承認を得てから着手した。

1. **「点線は何？」**: ゼロ基準線（推定在庫0の目印）である旨を回答。コード変更なし。
2. **「右の年月がかけている」**: 月ラベルが `text-anchor: middle` 一律だったため、末尾ラベルがSVG右端からはみ出して見切れていた。先頭は`start`、末尾は`end`にtext-anchorを個別設定して解消。
3. **「左にメモリが欲しい」**: Y軸の数量目盛りが存在しなかったため、最大値・最小値（0がその間にあれば0も）をカンマ区切りで表示するように追加。表示スペース確保のため`paddingLeft`を32→40に拡張。

- **ブランチ**: `develop` 上で直接実施（ステージ9、tasks.md タスク50〜55）。`origin` への push は未実施（明示指示待ち）
- **テスト**: `pytest` アプリ内 658件・リポジトリ全体 1673件 Green。`manage.py check` 問題なし。マイグレーション不要
- **変更ファイル**（既存改修）: `static/js/inventory-order-alert-list.js`（`renderAnchoredStockChart()` の月ラベルtext-anchor調整・Y軸目盛り追加）、`static/css/app.css`（`.ioa-anchored-stock-trend-y-axis-label` 追加）、`docs/spec/04_shipment-history-chart/design.md`（§6.6追記）、`tests/test_inventory_order_alert_list_js.py`（新規テスト2件: TC-SHC-X-016, X-017）
- Oracle・配信ペイロードへの影響: なし（表示のみの変更）

### 追記（2026/09/03、「まだ見切れてる」への再対応・ステージ10）

ステージ9で月ラベルの見切れを修正したはずだったが、ユーザーから「まだ見切れてる」と再指摘を受けた。あわせてExcelグラフの画像を提示され、Y軸目盛りを等間隔の複数目盛り線にしたい旨が伝わった。

- **見切れが直っていなかった根本原因**: `label.setAttribute("text-anchor", "end")` で設定していたが、SVGではCSSクラス（`.ioa-anchored-stock-trend-axis-label { text-anchor: middle; }`）の方が優先度が高く、setAttributeでの上書きが無視されていた。**`label.style.textAnchor = "end"`（インラインstyle）に変更**して解消した。
- **Y軸目盛りの方式**: AskUserQuestionで「最大値・最小値のみ（現状維持）」と「等間隔の複数目盛り線（推奨）」を提示し、後者を選択いただいた。`GRID_LINE_COUNT = 4`（値域を4分割=5本の目盛り線）で実装し、Excelのデフォルトに近い見た目にした。
- **ゼロ基準線の扱い**: グリッド線導入に伴い、ゼロ基準線（破線）は「マイナス域が実際にある場合のみ」描画するよう条件を追加した。全点0以上の行では最下段のグリッド線が0を兼ねるため、重複を避けた。
- **ブランチ**: `develop` 上で直接実施（ステージ10、tasks.md タスク56〜61）。`origin` への push は未実施（明示指示待ち）
- **テスト**: `pytest` アプリ内 659件・リポジトリ全体 1674件 Green。`manage.py check` 問題なし。マイグレーション不要
- **変更ファイル**（既存改修）: `static/js/inventory-order-alert-list.js`（text-anchorのstyle上書き化、等間隔グリッド線描画への置き換え）、`static/css/app.css`（`.ioa-anchored-stock-trend-grid-line` 追加）、`docs/spec/04_shipment-history-chart/design.md`（§6.6再改訂）、`tests/test_inventory_order_alert_list_js.py`（TC-SHC-X-016改訂、TC-SHC-X-018新設）
- **教訓**: SVG手組み実装で `setAttribute` によるpresentation attributeの上書きはCSSクラス指定に負けることがある。確実に上書きしたい場合は `element.style.xxx`（インラインstyle）を使うこと。

### 追記（2026/09/03、「MARI視点のグラフは削除して」への対応・ステージ11）

推定在庫推移グラフ（ステージ7でSLIMS起点・MARI起点の2系列として実装）について、ユーザーから「ここでの、MARI視点のグラフは削除して」との指示を受けた。事前に変更対象ファイル・影響範囲を提示し承認を得てから着手した。

- **撤去した範囲**: 「推定在庫推移」グラフのMARI起点系列（緑系の折れ線・凡例項目）のみ。**「在庫数(MARI)」欄自体（詳細ダイアログ上部の数値表示、§4.1.6「在庫」区分）は変更していない**。MARI在庫データの取得処理（`fetch_mari_stock_totals()`等）にも影響なし。
- **過去の決定との関係**: ステージ7着手時点でユーザーから明示的に「SLIMS在庫起点とMARI在庫起点の２つを表示して」との指示を受けて2系列にした経緯がある。今回はその決定を覆す新たな指示であり、そのまま反映した（SDDの原則どおり、旧要件・旧設計は削除せず「撤去済み」注記で履歴を残した）。
- **実装への影響**: `renderAnchoredStockChart(sectionEl, slimsSeries, mariSeries)` から `mariSeries` 引数を削除し `renderAnchoredStockChart(sectionEl, slimsSeries)` に簡略化。`fillDetailSections()` から `mariAnchor`/`mariAnchoredTrend` の算出を削除。`buildAnchoredStockTrend()`（算出関数）自体はSLIMS/MARIを区別しない汎用実装のため変更不要だった。
- **ブランチ**: `develop` 上で直接実施（ステージ11、tasks.md タスク62〜67）。`origin` への push は未実施（明示指示待ち）
- **テスト**: `pytest` アプリ内 661件・リポジトリ全体 1676件 Green。`manage.py check` 問題なし。マイグレーション不要
- **変更ファイル**（既存改修）: `static/js/inventory-order-alert-list.js`（`renderAnchoredStockChart()`・`fillDetailSections()` からMARI関連コードを削除）、`static/css/app.css`（MARI系列専用スタイルを削除）、`docs/spec/04_shipment-history-chart/requirements.md`・`design.md`・`test-design.md`（REQ-SHC-F-006等に撤去注記）、`docs/ubiquitous_language.md`（V-218定義改訂）、`docs/在庫発注アラート_機能仕様書.md`（§4.1.6・改訂履歴4.12）、`tests/test_inventory_order_alert_list_js.py`（TC-SHC-X-019, X-020新設）

### 追記（2026/09/11、「MARIの入荷の棒グラフを足して」「SLIMSに在庫がないものは0として履歴を作成して」への対応・ステージ12）

推定在庫推移グラフについて、ユーザーから2点の指示を受けた。事前に変更対象ファイル・影響範囲を提示し承認を得てから着手した。
着手前に「SLIMS側には出荷履歴はない？」という質問を受けており、**SLIMS CSV は取込時点の在庫スナップショット（`WSHOCD`/`WLOCCD`/`WKSBQT`/`WNYUDT`/`WMFGLT`）のみで時系列を持たず、取込ごとに洗い替えられる**こと、したがって推定在庫推移は**起点だけが SLIMS・増減は MARI（`T_SHIP`/`T_PAST_INSPC_ACPT`）由来**であることを回答した上での指示である。

- **入荷グラフの形式（対話で確認済み）**: AskUserQuestion で「推定在庫推移に重ねる（同一Y軸）」「別区分として下に追加」「重ねて右側に第2Y軸」の3案を提示し、**「推定在庫推移に重ねる（同一Y軸）」** を選択いただいた。在庫数・入荷数量はいずれもバラ数で単位が同じため第2Y軸は設けず、軸を1つに保った（軸が2つあると棒と線の高さを相対比較できると誤読されるため）。
- **ステージ8の決定との関係**: 2026/09/03 に「入出荷のグラフはいらない」との指示で「入出荷推移」区分（出荷=青・入荷=橙の2系列折れ線）を丸ごと撤去した経緯がある。今回はその決定を部分的に覆す新たな指示だが、**独立した区分は復活させず、入荷のみを推定在庫推移グラフ上の棒として重ねる**形にした（撤去した区分そのものの復活ではない）。SDDの原則どおり、旧記述は削除せず「表示再開」注記で履歴を残した。**出荷推移（V-216）は引き続きグラフに描かない**（算出材料のみ）。
- **0 起点の適用範囲（対話で確認済み）**: 在庫数(SLIMS) には 3 状態（値あり／該当なし＝空／未取得＝`－`、`stock_quantity.py`）がある。AskUserQuestion で範囲を確認し、**「該当なし（空）のみ 0 起点」** を選択いただいた。未取得（`－`）は SLIMS を参照できていない状態であり、「在庫ゼロ」と描くと実際には在庫がある品目をゼロと見せてしまうため、従来どおり「推定在庫推移を算出できません。」を表示する。**V-206（在庫数(SLIMS)）の表示規則そのものは変更していない**（空は空、`－` は `－`。0 起点は V-218 の算出に限った扱い）。
- **棒の描画順序**: グリッド線 → **棒** → 折れ線・データ点 → 月ラベル の順に `append` し、棒が折れ線を隠さないようにした。色は橙系（`#f59e0b`、`fill-opacity: 0.75`）で、折れ線のインディゴと区別した（ステージ6で撤去した旧「入荷」系列と同じ橙系を再利用し、ユーザーの色の記憶と揃えた）。棒幅は `stepX * 0.5`、数量 0 の月は棒を描かない。
- **Y軸値域への算入**: 入荷が推定在庫を上回る月でも棒が切れないよう、`maxQty` の算出に入荷数量を算入した。ゼロ基準線・等間隔グリッド線（ステージ9・10）の仕様は変更していない。
- **数値計算の検証方法**: JS用のテストランナーがないため、自動テストは従来どおりソース文字列アサーション（TC-SHC-X-021〜X-026、6件新設）。数値の正しさは Node で `parseAnchorQty`／`buildAnchoredStockTrend` の実コードを切り出して手動検証した（3状態の戻り値、0起点の逆算値、棒のゼロ基準Y座標・高さ、値域への入荷算入を確認）。恒久的な自動テストとして残っていない点はステージ7からの継続課題。
- **既存テストの改訂**: TC-SHC-X-020 は `renderAnchoredStockChart(anchoredStockTrendSection, slimsAnchoredTrend)`（引数2つ）を期待していたため、第3引数 `incomingTrend` を加えた新シグネチャに合わせて改訂した。MARI**起点系列**を持たないことの検証は `mariAnchor`／`mariAnchoredTrend` の不在で引き続き担保している。
- **キャッシュバスター**: `app.css` と `inventory-order-alert-list.js` / `-client.js` のクエリを `v=20260911-incoming-bars` に更新した（旧CSS/JSがブラウザキャッシュから使われると変更が見えないため）。本番反映時は `collectstatic` が必要。
- **ブランチ**: `develop` 上で直接実施（ステージ12）。`origin` への push は未実施（明示指示待ち）
- **テスト**: `pytest` アプリ内 598件・リポジトリ全体 **1682件 Green**。`manage.py check` 問題なし。マイグレーション不要
- **Oracle・配信ペイロードへの影響**: **なし**。入荷推移は既に配信済みのデータ（`getIncomingTrend`）を描画するだけで、Python 側（domain / use_cases / infrastructure）は無変更
- **変更ファイル**: `docs/ubiquitous_language.md`（V-217 表示再開・V-218 に0起点規定・改定履歴）、`docs/在庫発注アラート_機能仕様書.md`（§4.1.6・改訂履歴4.13）、`docs/spec/04_shipment-history-chart/design.md`（§6.5・§6.6）、`docs/spec/04_shipment-history-chart/test-design.md`（TC-SHC-X-020改訂、X-021〜X-026新設）、`static/js/inventory-order-alert-list.js`、`static/css/app.css`、`templates/inventory_order_alert/list.html`、`templates/base.html`、`tests/test_inventory_order_alert_list_js.py`

### 追記（2026/09/11、「詳細画面に5年９組の検索結果を別タブで表示するボタンを追加して」への対応・ステージ13）

ユーザーから「場所は推定在庫推移グラフの下で、年月は今月」と条件つきで指示を受けた。事前に変更対象ファイル・影響範囲を提示し承認を得てから着手した。

- **遷移先の判断**: 「検索結果を表示する」との指示のため、検索条件の入力画面（`/app/production/five-year-nine`）ではなく**検索結果ページ**（`/app/production/five-year-nine/result`）へ GET パラメータ付きで直接遷移する。`result_page` は `custCode` / `custItem` / `yearMonth` / `optionChange` / `asOfDate` / `months` を GET で受け取る既存実装で、**5年9組側のコード変更は不要**だった。
- **対象日付（asOfDate、対話で確認済み）**: AskUserQuestion で「今日の日付」「今月の1日」を提示し、**「今日の日付」** を選択いただいた。5年9組の検索画面は読み込み時に `asOfDate` を今日（`todayIsoDate()`）で埋めるため、ボタン経由と手入力で結果が一致する。パラメータを省略すると 5年9組側の既定は `date(year, month, 1)`（年月の1日）になり食い違うため、**明示的に今日を渡す**。
- **設変値（optionChange）**: 在庫発注アラートは設変値を持たないため**渡さない**。`validate_search_params()` は未指定時に既定 `*`（ワイルドカード）を適用する（実際に Python 側で評価して確認済み）。将来 5年9組の既定が変わったら自動的に追随する利点もある。
- **権限判定を入れなかった判断**: 5年9組は menu_key `five-year-nine` でミドルウェア制御されており、権限のないユーザーが押すと 403 の別タブが開く。当初は `can_access_menu_item()` でボタンを出し分ける案を推奨したが、ユーザーから **「在庫発注アラートと5年９組は同じアクセス権なので常に表示して」** との回答を得たため、出し分けは実装していない（`interfaces/views.py` は無変更）。**運用でアクセス権が分かれた場合はボタンの出し分けが必要になる**点を、この判断の前提として記録しておく。
- **日付をローカル時刻で組み立てた理由**: `toISOString()` は UTC 変換されるため、JST の 0:00〜8:59 に「前日」「月初なら前月」の値になる。`getFullYear()` / `getMonth()` / `getDate()` をゼロ埋めして組み立て、テスト（TC-SHC-X-031）で `toISOString` の不使用を固定した。Node で 2026/09/11 0:30 の生成結果が `yearMonth=2026-09&asOfDate=2026-09-11` になることを確認済み。
- **リンクを隠す条件**: 5年9組は得意先コード・得意先品番の両方を検索条件に要するため、どちらかが空の行では行（`ioa-detail-gonen-link-row`）を `hidden` にする。得意先コードが**数字3桁でない**行はブロックせず、5年9組側のエラーメッセージ（「得意先コードは数字3桁で指定してください。」）に委ねた（在庫発注アラート側に 5年9組のバリデーション規則を複製したくないため）。
- **パスをハードコードにした判断**: `{% url 'gonenkukumi:result_page' %}` ではなく JS 側の定数 `GONEN_RESULT_PATH` とした。href の組み立てがクライアント側（`fillDetailSections()`）で行データから行われるためで、既存の `CONFIRMATION_API` / `CONFIRMATION_MEMO_API` と同じ流儀に揃えた。**5年9組側の URL が変わった場合はこの定数の追随が必要**（コンテキスト境界をまたぐ結合点として認識しておく）。
- **検証方法**: JS 側は Node で実コードを切り出して生成 URL を確認（今日・年始・深夜0時台・キー欠落の4ケース）。サーバ側は `validate_search_params()` に生成した4パラメータを渡し、`cust_code=100` / `cust_item=90170-08026` / `year_month=2026-09` / `as_of_date=2026-09-11` / `option_change='*'` として解釈されることを確認した。
- **ブランチ**: `develop` 上で直接実施（ステージ13）。`origin` への push は未実施（明示指示待ち）
- **テスト**: `pytest` リポジトリ全体 **1689件 Green**（新規テスト7件: TC-SHC-X-027〜X-032 と配線確認1件）。`manage.py check` 問題なし。マイグレーション不要
- **Oracle・配信ペイロードへの影響**: **なし**（表示のみの変更。5年9組の Oracle クエリは遷移先で従来どおり実行される）
- **変更ファイル**: `docs/在庫発注アラート_機能仕様書.md`（§4.1.6「5年9組への遷移」新設・改訂履歴4.14）、`docs/spec/04_shipment-history-chart/design.md`（§6.5構成図・§6.7新設）、`docs/spec/04_shipment-history-chart/test-design.md`（TC-SHC-X-027〜X-032）、`templates/inventory_order_alert/list.html`、`static/js/inventory-order-alert-list.js`、`static/css/app.css`、`templates/base.html`、`tests/test_inventory_order_alert_views.py`、`tests/test_inventory_order_alert_list_js.py`

### 追記（2026/09/11、「推定在庫推移でマイナスの在庫があるのだけどなんで？」への調査と対応・ステージ14）

ユーザーから推定在庫推移のマイナスについて質問を受け、品番 **96160-00500** を提示された。実データ（2026-09-07 取込、`SISJB0001003000109054920.csv`、5,640 行 / 集計 2,298 行）を PostgreSQL の集計スナップショットから直接読み、原因を特定した上で対応した。

**調査結果（96160-00500、得意先100 トヨタ車体の行）**

| 項目 | 値 |
|---|---|
| 在庫数(SLIMS) | 12,970（2F5-20-5 に 10,000@2024/12/23・2,000@2024/12/05、2F5-22-4 に 800、2D5-22-1 に 170@2021/10/21） |
| 入荷（MARI 検収・24か月） | 2024-11 10,000 / 2024-12 20,000 / 2025-04 10,000 = 40,000 |
| 出荷（得意先100・24か月） | 全月 0（最終出荷 2023/07/27 = ウィンドウ外） |
| 逆算結果 | 2024-12 まで 2,970 → 2024-11 **-17,030** → 2024-10 **-27,030** |

マイナス 27,030 は「入荷 40,000 − 在庫 12,970 − 出荷 0」。原因は2つに分解できた。

- **① 入荷の重複計上（主因・構造的な欠陥）**: 入荷は `incoming_by_pair.get((level1_item, level1_vend))` で引くため、**同じ内作品番 × 仕入先を共有する複数行に同じ全量が現れる**。一方 出荷は得意先 × 得意先品番で分割される。96160-00500 は5つの得意先（100/104/135/137/176）で共通で、入荷 40,000 が5行すべてに入る一方、出荷は 8,000 / 3,600 / 0 / 0 / 0（計 11,600）に分かれていた。出荷0の行では入荷 40,000 がまるごとマイナスに効く。より極端な例が 94223-80600 で、入荷 78,960,000 が4行に重複し、小口の得意先の行が **-7,500万** まで沈んでいた。
- **② 出荷では説明できない残差**: 全得意先の出荷を足しても `12,970 + 11,600 − 40,000 = -16,430` で釣り合わない。手がかりとして **SLIMS 在庫の入荷日に 2025 年のロットが1つもない**（最新 2024/12/23）のに MARI は 2025/04/02 に 10,000 を検収している。生産投入・棚卸差異・形式外ロケーション（`WLOCCD` の形式で絞るため取込対象外になる在庫）のいずれかで、**実態の不一致を示すシグナルとして残すのが正しい**と判断した。
- **単位違いの可能性は否定**: SLIMS 側に 10,000 のロットが実在し、MARI の 10,000 / 20,000 と桁が揃っているため、この品番では単位の不一致は考えにくい（前回の回答で挙げた可能性を実データで却下した）。

**対応（対話で確認済み）**

AskUserQuestion で「得意先品番単位で逆算する（推奨）」「注記を追加するだけ」「まずは調査を優先」「マイナスを0でクランプ」の4案を提示し、**「得意先品番単位で逆算する」** を選択いただいた。

- **算出単位を得意先品番に変更**した。起点の在庫数(SLIMS) が得意先品番単位であるのに、出荷は得意先単位・入荷は内作品番単位という**3つの粒度が混在していたことが根本原因**である。出荷を同一得意先品番の全得意先分で合算し、入荷を `(level1_item_cd, level1_vend_cd)` の重複を除いて合算することで、3者を同じ粒度に揃えた。
- **実装場所**: クライアント側（`getItemTrends(itemCd)` を list-client.js に新設）。全行は既に配信済みのため、**Oracle 問い合わせ・配信ペイロード・Python 側はいずれも無変更**。サーバ側で合算して配信すると 1 行あたり約 1.6KB × 2,298 行の増加になるため採らなかった。
- **フィルタ非依存**: 合算は `applyListFilters()` を通す前の `allRows` で行う。物理的な在庫・入荷は画面の絞り込みと無関係で、フィルタを変えるとグラフが変わってしまうため。テスト（TC-SHC-X-036）で `applyListFilters` を参照しないことを固定した。
- **同一品番の行はすべて同じグラフになる**（意図した挙動）。在庫数・入荷数量は元々品番単位で共有されており、行ごとに違うグラフを描くと数量の整合が取れない。この点は仕様変更として ubiquitous_language.md V-218 / 機能仕様書 §4.1.6 に明記した。
- **キャッシュ**: 品番ごとに `Map` で合算結果をキャッシュする（ダイアログを開くたびに 2,298 行を走査しないため）。

**実データでの検証（出荷するコードそのものを Node で評価）**

集計スナップショットを `row_to_client_dict()` でクライアント形式に変換して JSON に書き出し、`buildItemTrends()` / `parseAnchorQty()` / `buildAnchoredStockTrend()` の**実コードを切り出して**全 2,298 行に適用した。

| | 改訂前 | 改訂後 |
|---|---|---|
| マイナスを含む行 | 443 行（19.3%） | **240 行（10.4%）** |
| 96160-00500 の最小推定値 | -27,030 | **-16,430** |
| 94223-80600 の最小推定値 | -72,140,000 | **+939,500（マイナス解消）** |

同一得意先品番の全行で結果が一致することも確認した。

- **ブランチ**: `develop` 上で直接実施（ステージ14）。`origin` への push は未実施（明示指示待ち）
- **テスト**: `pytest` リポジトリ全体 **1694件 Green**（新規テスト5件: TC-SHC-X-033〜X-037）。`manage.py check` 問題なし。マイグレーション不要
- **変更ファイル**: `docs/ubiquitous_language.md`（V-218 算出単位・V-216/V-217 補足・改定履歴）、`docs/在庫発注アラート_機能仕様書.md`（§4.1.6・改訂履歴4.15）、`docs/spec/04_shipment-history-chart/design.md`（§6.6 算出単位）、`docs/spec/04_shipment-history-chart/test-design.md`（TC-SHC-X-033〜X-038）、`static/js/inventory-order-alert-list-client.js`、`static/js/inventory-order-alert-list.js`、`templates/inventory_order_alert/list.html`、`tests/test_inventory_order_alert_list_js.py`

**残課題（要確認）**

1. **残るマイナス 240 行（10.4%）の実態確認**。96160-00500 の 2025/04/02 検収 10,000 が SLIMS 在庫に見当たらない件など、MARI と SLIMS の実物照合が必要。アプリ側の計算ではなく業務側の不一致である可能性が高い。
2. **形式外ロケーションの在庫が取込対象外**（`WLOCCD` が `[0-9][0-9A-Z][0-9]-\d{2}-\d` 形式の行のみ集計）。仮置き場等に在庫がある場合、在庫数が実態より小さく出てマイナスの一因になりうる。SLIMS CSV の原本で形式外ロケーションの有無を確認したい。
3. **MARI の数量列の単位定義が未文書化**。`T_SHIP.SHIP_QTY` / `T_PAST_INSPC_ACPT.INSPC_ACPT_QTY` の単位はどの仕様書にも明記がなく、SLIMS 在庫数（バラ数）と同一単位である前提で同じY軸に置いている。96160-00500 では整合していたが、全品番での確認は未実施。

### 追記（2026/09/11、SLIMS のロット残数という業務知識を受けた再調査・ステージ15）

ステージ14 の残課題①に対し、ユーザーから **「SLIMS の入荷表記は、例: 10/4 に 1000 個入荷（棚入れ）、そこから 50 個出荷したら 10/4 のデータが 950 個になる」** という業務知識をいただいた。これを前提に再調査したところ、**ステージ14 で挙げた残課題①②はいずれも誤りだった**ことが判明し、算出単位をさらに改めた。

**ステージ14 の誤りの訂正**

1. **「2025/04/02 検収の 10,000 が SLIMS 在庫に見当たらない」は誤り**。SLIMS の入荷日つき在庫は**ロットの残数**であり、残数 0 になったロットは行ごと消える。したがって「入荷日 2025/04 の行が無い」ことは「未計上」ではなく「**全量払い出された**」ことを意味する。仕様書に SLIMS の入荷日の性質が書かれていなかったため誤読した。**同じ誤読を防ぐため、V-205 在庫内訳・V-201 最終入荷日・機能仕様書 §4.1.2 にこの性質を明記した。**
2. **「棚卸差異・生産消費・形式外ロケーションの可能性」も誤り**（少なくとも 96160-00500 では該当しない）。実データを追い直したところ、**同じ内作品番 96160-00500-9065（仕入先 9065 ミズタニ）を、別の得意先品番 10523-X0A02 でも使っていた**。

| 得意先品番 | 在庫数(SLIMS) | 出荷計(24か月) | 入荷計 |
|---|---|---|---|
| 96160-00500 | 12,970 | 11,600 | 40,000（共有） |
| 10523-X0A02 | 19,000 | 1,000 | 40,000（同じもの） |
| 合計 | **31,970** | **12,600** | **40,000** |

`31,970 + 12,600 − 40,000 = +4,570` で釣り合う。**入荷 40,000 のうち 19,000 が 10523-X0A02 側の在庫として残っていただけ**で、在庫が消えていたわけではなかった。

**算出単位の再改訂（対話で確認済み）**

AskUserQuestion で「内作品番を共有する品番をまとめる」「現行のまま＋共有相手を表示」「両方」「変えない」の4案を提示し、**「内作品番を共有する品番をまとめる」** を選択いただいた。

- **根本原因は3つの粒度の不一致**だった。在庫＝得意先品番、出荷＝得意先×得意先品番、入荷＝内作品番×仕入先。ステージ14 で在庫と出荷を得意先品番に揃えたが、**入荷は依然として内作品番単位のまま**だったため、1つの内作品番を複数の得意先品番が共有する場合は片方の在庫しか起点に入らず、入荷の全量を引いていた。
- **照合単位** = 得意先品番と (内作品番, 仕入先) の組を節点とする**二部グラフの連結成分**。1つの得意先品番が複数の組に紐づく場合（94223-80600 は 4 組）と、1つの組を複数の得意先品番が共有する場合（96160-00500 と 10523-X0A02）の両方があるため、単純な「品番でグループ化」では閉じない。Union-Find（経路圧縮のみ）で全行から1度だけ構築してキャッシュする。
- **起点の解釈は呼び出し側に残した**。在庫数の3状態（値あり／該当なし／未取得）の解釈は `parseAnchorQty()` が持っているため、クライアント側は `stocks`（得意先品番と在庫の表示文字列）を返すだけにし、パーサを二重に持たない構造にした。未取得の扱いは「0 として加算するが、単位内の全品番が未取得なら非表示」とし、従来の非表示動作を保った。
- **起点の内訳表示**: 照合単位が複数の得意先品番を含むと、グラフ起点が詳細ダイアログの「在庫数(SLIMS)」の表示値（当該品番分のみ）と食い違う。該当するのは実データで **25 単位・144 行（6.3%）** のみ。この場合だけグラフ直下に「グラフの起点 31,970 = 96160-00500 12,970 ＋ 10523-X0A02 19,000（同じ仕入先品番を共有する 2 品番の在庫を合算しています）」を出す。単一品番（1,969 単位）では冗長なので出さない。**最大のクラスタは 37 品番**を含むため、先頭5品番＋「他N品番」に丸める。

**実データでの検証（出荷するコードそのものを Node で評価）**

| 集計単位 | マイナスを含む行 | 96160-00500 の最小推定値 | 94223-80600 の最小推定値 |
|---|---|---|---|
| 行単位（当初） | 443 行（19.3%） | -27,030 | -72,140,000 |
| 得意先品番単位（ステージ14） | 240 行（10.4%） | -16,430 | +939,500 |
| **照合単位（ステージ15）** | **210 行（9.1%）** | **+3,570** | +939,500 |

96160-00500 と 10523-X0A02 が同一の照合単位（起点 31,970 / 出荷計 12,600 / 入荷計 40,000）に属し、同じ結果になることも確認した。

- **ブランチ**: `develop` 上で直接実施（ステージ15）。`origin` への push は未実施（明示指示待ち）
- **テスト**: `pytest` リポジトリ全体 **1697件 Green**（TC-SHC-X-033〜X-037 を改訂、X-039〜X-041 を新設）。`manage.py check` 問題なし。マイグレーション不要
- **Oracle・配信ペイロード・Python 側への影響**: **なし**（`level1_item_cd` 等は既に配信済み。合算はクライアント側の処理）。CSS も変更なし
- **変更ファイル**: `docs/ubiquitous_language.md`（V-201・V-205 にロット残数の性質、V-218 の算出単位、改定履歴2件）、`docs/在庫発注アラート_機能仕様書.md`（§4.1.2・§4.1.6・改訂履歴4.16）、`docs/spec/04_shipment-history-chart/design.md`（§6.6）、`docs/spec/04_shipment-history-chart/test-design.md`（TC-SHC-X-038改訂、X-039〜X-041新設）、`static/js/inventory-order-alert-list-client.js`、`static/js/inventory-order-alert-list.js`、`templates/inventory_order_alert/list.html`、`tests/test_inventory_order_alert_list_js.py`

**残課題（更新）**

1. **残るマイナス 210 行（9.1%）の実態確認**。ステージ14 の残課題①②は本ステージで解消・訂正済み。残りは 24 か月ウィンドウの境界（窓より前の在庫・出荷）や、本当の棚卸差異・生産消費の可能性がある。必要なら該当行の一覧を出力できる。
2. **MARI の数量列の単位定義が未文書化**（`T_SHIP.SHIP_QTY` / `T_PAST_INSPC_ACPT.INSPC_ACPT_QTY`）。SLIMS 在庫数（バラ数）と同一単位である前提で同じY軸に置いている。96160-00500 では照合単位で釣り合ったため、この品番については単位が一致していることの裏づけが得られた。

---

### 追記（2026/09/16、05_single-flow-view 第 1 段階の実装・ステージ17）

判定軸（V-210）を廃止し判定期間（V-211）を 1/3/5 年に一本化、流動区分（S-203）を **低流動品（入荷なし）／低流動品（出荷なし）** に改称、セルに 状況／推奨アクション（T-207）／責任部署（R-201）を表示する第 1 段階（`docs/spec/05_single-flow-view/tasks.md` タスク 1〜40）を実装した。タスクごとの判断は tasks.md の実行レポートに記録し、ここでは実データでの測定と横断的な判断だけを残す。

**実データでの件数変化（2026/09/07 取込スナップショット、2,298 行、R-3）**

| 判定条件 | 低流動品（入荷なし） | 在庫死蔵品 | 低流動品（出荷なし） | 通常流動品 | 対応要（通常以外） |
|---|---|---|---|---|---|
| 旧 既定（低流動判定軸・3 か月） | 250 | 903 | 111 | 1,034 | 1,264（55.0%） |
| **新 既定（判定期間 1 年）** | **293** | **613** | **93** | **1,299** | **999（43.5%）** |
| 判定期間 3 年 | 329 | 209 | 48 | 1,712 | 586 |
| 判定期間 5 年 | 343 | 0 | 0 | 1,955 | 343 |

- メニュー画面のアラート帯（固定基準）は **旧 250 / 903 / 111 → 新 293 / 613 / 93** に変わる。在庫死蔵品が 290 件減り、低流動品（入荷なし）は 43 件増える（3 か月では「出荷あり」だった行のうち、出荷が 3 か月〜1 年前の行が入荷なし側へ移るため）。周知文はこの増減を前提に書いた（`Document/本番環境デプロイ手順.md` §11.1）
- 受け入れ基準 #2 の **96160-00500**（最終入荷 2025/04/02・最終出荷 2026/06/15）は 1 年で低流動品（入荷なし）、3 年・5 年で通常流動品。状況は「出荷は継続、最終入荷 2025/04/02（1年以上入荷なし）」、推奨アクションは「仕入先へ生産継続可否・設備/金型の有無を確認。在庫切れ予測月が近いものから」、責任部署は「調達G・営業G・生産管理」と出ることを実データで確認した
- 開発 DB の確認記録（確認中・確認済み）は **0 件**だったため、確認状態リセットの影響はローカルでは測れない。本番は手順書 §11.1 に従う

**計画からの逸脱・横断的な判断**

1. **`RowCounts` の項目名改称の波及**（タスク 14）。`supply_risk` / `excess_stock_risk` を `low_flow_no_incoming` / `low_flow_no_shipment` に改称し、API 応答の JSON キーも `supplyRisk` / `excessStockRisk` → `lowFlowNoIncoming` / `lowFlowNoShipment` に変えた。tasks.md はこの改称を「暫定維持」としていたが、タスク 1 の edge_cases テストとタスク 19 の旧識別子走査が残存を許さないため、`infrastructure/persistence/summary_snapshot_repository.py`・`summary_repository.py`、`use_cases/save_confirmation.py`・`reset_confirmations.py`・`summary_api.py`・`portal_dashboard.py` を同時に改称した。**外部から本 API（`/api/inventory-order-alert/summary` 等）の `counts` を参照している呼び出しがあれば追随が必要**（社内ポータル内の JS は追随済み）
2. **判定ルールダイアログの状況列は `{period}` を選択中の判定期間に置換して表示**する。設計は「テンプレートのまま」も許容していたが、セルと同じ文言で読める方が分かりやすいと判断した（`data-status-template` を残し JS が置換）
3. **`build_flow_quadrant_rule_rows(recommended_actions=...)`** に省略可能な引数を設け、定義ファイルで上書きした推奨アクションが判定ルールダイアログにも反映されるようにした（test-design は引数なしの呼び出しを想定。互換は維持）
4. **CSV の `判定軸` 列は domain 側で常に空**にした（`export_csv.EMPTY_COLUMNS`）。use case で空文字を詰める方式より、列定義と挙動が同じ場所にある方が読みやすい。流動区分の値も domain 側で `normalize_flow_quadrant` を通し、旧称が残る行でも新称で出す
5. **推奨アクション定義ファイルは置かない**（`infrastructure/config/recommended_actions.json` は未作成。`recommended_actions.example.json` を雛形として同梱）。既定文言は用語集 S-203 の表そのもの。文言を業務側と確定したらファイルを置くか、既定を書き換える
6. **`flow_quadrant.responsible_departments()` は `recommended_action.py` を関数内 import** で参照する（相互参照の回避）。定義表を 1 か所にする設計を優先した

**検証**: `pytest`（リポジトリ全体）**1,825 件 Green**、`manage.py check` 問題なし、`node --check` 両 JS 通過。マイグレーション不要。`git status` に意図しない変更なし（09/03・09/11 の未コミット分は本作業とは別で、着手前から存在）。

**未実施（ユーザー確認事項）**

- **ブラウザでの目視確認**は未実施。判定期間セレクタの切替でセル・件数・判定ルールダイアログ・詳細ダイアログが追随すること、狭幅（1100px 以下）で状況・責任部署が隠れること、行背景色が旧区分の色のまま出ることは実機で確認が必要
- `develop` へのコミット・push は未実施（明示指示待ち）
- 第 2 段階（タスク 41〜68、内示受注に基づく需要予測・緊急度）は着手可否の確認待ち

---

### 追記（2026/09/17、05_single-flow-view 第 2 段階の実装・ステージ18）

内示受注（`T_UNCNFM_ODR`）に基づく需要予測（V-220）・在庫切れ予測月（V-221）・在庫月数（V-222）・緊急度（V-223）を実装した（`docs/spec/05_single-flow-view/tasks.md` タスク 41〜68）。

**実データでの数値検証（test-design §3.3。2026/09/17 基準日、Oracle 読み取りのみ・スナップショットは更新していない）**

| 検証 | 期待（test-design） | 実測 |
|---|---|---|
| 照合単位の構築 | 1,994 単位、複数品番 25 単位・144 行（04 の JS） | **2,031 単位、複数品番 24 単位・75 行、最大 2 品番**（2,299 行） |
| 96160-00500 の需要予測 | 内示、10 月 224、在庫合計 31,970、在庫月数 ≒ 150 | **内示、(224, 216, 197)、31,970、150.6 か月、在庫切れ予測月 None（120 か月超＝十分）** |
| 低流動品（入荷なし）で内示のある割合 | 約 74% | **296 行のうち 内示 126（43%）/ 実績ベース 166 / なし 4** |
| 取込時間の増分（R-6） | +5 秒以内 | **`fetch_unconfirmed_orders` 4.33 秒**（109,589 明細・1,373 組）、`attach_demand_forecast` 0.34 秒。集計全体は 85 秒 |
| 単位（バラ数）の整合（R-5） | 在庫月数の上位/下位 10 件を目視 | 下位は在庫 0 の行（在庫月数 0.0・当月に在庫切れ）。上位は 3,448〜18,788 か月（例 730420-1320: 在庫月数 18,788.7、内示ベース）。**桁違いの単位は「大量在庫 × 極小需要」で説明がつく範囲**だが、内示数量の単位（バラ数か箱数か）は未確認 |
| 配信ペイロード増分 | 行あたり 100 バイト程度（design §5.2） | **約 500 バイト/行**（内示推移 4 件と算出値をすべて持つため）。2,299 行で約 1.1MB |

**照合単位の件数が 04（JS）と違う理由**: 04 の JS 実装は組（内作品番×仕入先）が空の行も `P:|` という共通キーで連結するため、**仕入先が解決できない行がすべて 1 つの単位にまとまっていた**（144 行のうち約 70 行がこれ）。Python 実装（`reconciliation_unit.py`）では空の組を連結しない（D-087）。**JS 側の推定在庫推移は、仕入先未解決の行で無関係な品番の在庫・出荷を合算している可能性がある**（要確認 #7）。

**計画・設計からの逸脱**

1. **警告の扱い**: I-006 の「`aggregation_error` に追記」は採らず、`StockImportInfo.aggregation_warning` を新設して取込結果メッセージにだけ出す（`aggregation_error` が空でないと一覧が集計失敗扱いになるため）
2. **在庫切れ予測月の規則**: 「初めて負になる月」を厳密に適用し、test-design D-100 の期待値を 2027-06 → **2027-08** に修正（計算が合わなかった）。在庫 0 以下は当月
3. **実績ベースの月別需要**: 翌月〜翌々々月に月平均を置く（0 のままだと 3 か月間需要なし扱いになるため）
4. **在庫月数はソート専用項目**（一覧の列にしない）。`SORT_ONLY_COLUMNS` / `sortOnlyColumns` を新設し、並び替えダイアログにだけ出す
5. **内作品番が解決できない行**は既存規則どおり得意先品番で代替（`""` にしない）
6. **`demand_forecast_stock_total`** を行に追加（詳細ダイアログの内訳表示用。design §5.2 の表にはない）

**検証**: `pytest`（リポジトリ全体）**1,904 件 Green**、`manage.py check` 問題なし、`node --check` 両 JS 通過。マイグレーション不要（JSON 項目の追加のみ）。

**未実施（ユーザー確認事項）**

- ブラウザでの目視確認（緊急度行・需要予測区分・在庫月数ソート）は未実施
- **本番・開発 DB のスナップショットはまだ旧形式**。SLIMS CSV を再取込すると需要予測が付く。再取込するまで緊急度は表示されない（旧スナップショット互換、X-014）
- `develop` へのコミット・push は未実施

---

### 追記（2026/09/17、05 実装後のユーザー確認による一覧セルの簡略化・ステージ19）

実装をユーザーが画面で確認し、**「流動区分が長い。説明（現在の状況と対処方法）はクリックしたら出るように」「入荷実績なしマークは最終入荷日で分かるので不要」** の 2 点の指摘を受けた。AskUserQuestion で「常時表示は区分名のみ」「説明は詳細ダイアログに任せる（行クリック）」を選択いただき、次のとおり改めた。

- 一覧の流動区分セルは **区分名のみ**。状況・緊急度・推奨アクション・責任部署は詳細ダイアログの「流動区分」「需要予測」区分で示す（既に実装済みの項目のため、一覧側を外すだけ）
- **入荷実績なしバッジを撤去**。詳細ダイアログの区分名横の注記も外した（状況の文言「最終入荷 入荷実績なし」と CSV の `入荷実績なし` 列は据え置き）
- 仕様（REQ-SFV-F-004・F-005、design §6.3、test-design、機能仕様書 5.2）を先に改訂してから実装を変更した。tasks.md タスク 69〜72
- 05 design §6.3 で決めた `min-width: 22em` と狭幅ルールは不要になったため撤回

**トレードオフ**: 緊急度が一覧から見えなくなる。並び替え「在庫月数」で危険な行を上に集めることはできる。

---

## 要確認・要判断（優先度順）

### 1. 「在庫変動」を「出荷推移」に読み替えたこと 【最重要】

**元の依頼は「在庫変動のグラフ」だったが、実装したのは「出荷数量の月次推移グラフ」。**

- 理由: SLIMS 在庫数は取込のたびに洗い替えられ、MARI 在庫数も現在値のみで、在庫数そのものの時系列データが存在しない（既存コードの設計上の制約）。
- 直前の会話でユーザー自身が「出荷回数のデータでできない？」と提案し、それに対して私が「できる。出荷推移の方が判定根拠を可視化できる分むしろ良い」と回答し、実装方針として合意していた。
- **確認したいこと**: この読み替えで意図通りか。もし「それでも在庫数の推移が見たい」ということであれば、別途「取込ごとの集計スナップショットから在庫数を拾う」方式（不定期・まばらな点になる）か、「新しく時系列テーブルを作り今後真面目に積む」方式を改めて検討する。

### 2. 対象期間を「直近24か月」に固定したこと 【実測値により重要度が上がった】

- 判定期間（V-211）は死蔵判定軸で最大 **5年（60か月）** まである。24か月では、5年判定で「在庫死蔵品」になった行の判定根拠を、グラフの表示期間内で確認できない場合がある。
- 24か月にした理由: グラフの点数・配信データ量を抑えるための暫定値。明確な根拠に基づく値ではない。
- **実測結果（タスク17）**: 24か月ぶんの出荷推移で **1行あたり配信データ量が +794バイト**（見積りの約500バイトから乖離）。既存の1行あたりデータ量（実測約4,000バイト）に対し約2割増。**60か月に伸ばすと単純比例で約2,000バイト/行**になる見込みで、5,000行規模の一覧では合計 約10MBの増加になる。
- **確認したいこと**: 24か月のままでよいか（判定根拠を見せきれない可能性を許容する）、60か月まで伸ばすか（配信量が明確に増える）、あるいは判定軸に応じて可変にするか。

### 3. 入荷実績（入荷推移）をグラフに含めなかったこと 【解決済み・2026/09/03】

- ~~流動区分の判定は「入荷の有無」と「出荷の有無」の両方で決まるが、今回のグラフは**出荷のみ**を表示する。~~
- **2026/09/03、ユーザー指示「入荷も含めて」により対応済み**。入荷推移（V-217）を第2系列として追加し、区分見出しも「出荷推移」から「**入出荷推移**」に変更した（design.md §3.3・§4.3・§6.1〜§6.5、tasks.md ステージ6、機能仕様書 改訂履歴 4.9）。
- 追加時に生じた新規判断（対話で確認済みではなく、design.md 作成時の自己判断のため、念のため記録）:
  - **数量フィールドの選択**: `T_PAST_INSPC_ACPT` には `ACPT_QTY`（受入数量）と `INSPC_ACPT_QTY`（検収数量）の2つの数量列がある。「検収済み＝正式に受け入れた数量」がより意味のある指標と判断し **`INSPC_ACPT_QTY`** を採用（ubiquitous_language.md V-217 に明記）。
  - **突合キーの違い**: 出荷推移は「得意先×得意先品番」で突合するが、入荷推移は「BOM階層1構成子×仕入先」で突合する（既存の最終入荷日解決ロジックと同じ軸）。突合先が解決できない行（BOM未解決等）は入荷推移が全月0になる。
  - **新規 Oracle クエリの追加**: 出荷推移はゼロ追加クエリで実現できたが、入荷推移は個々の検収明細（月次集計に必要な数量つき）を取る手段が既存になく、`fetch_incoming_receipts()` を新設した。既存の `fetch_last_incoming_by_item_vend()`（`MAX(ACPT_DATE)` の集約のみ、範囲指定なし）を安易に真似ず、**`WHERE ACPT_DATE >= :window_start` で直近24か月に絞った**（REQ-SHC-NF-008）。取込ごとに1回のみ発行。
  - **命名債務（意図的に許容）**: `build_monthly_shipment_trend()`（domain関数）と `group_shipments_by_pair()`（infrastructure関数）を、名前を変えずに入荷推移でも汎用再利用した。「出荷」という名前が残ったまま入荷にも使う点は将来の可読性を損なうが、既にテスト済み・コミット済みのコードへの改名リスクとレビュー範囲拡大を避けるため許容した（design.md R-6）。同様に JS の `renderShipmentTrendChart` 関数名、CSS/HTML の `ioa-detail-shipment-trend-*` クラス名も維持した。
  - **配信ペイロード増分**: 入荷推移単独 +793バイト/行、出荷+入荷合計 +1,587バイト/行（実測、design.md §6.2）。項目2（対象期間24か月固定）の判断により重要度が増す（60か月化すると合計で約4,000バイト/行になる見込み）。

### 4. グラフの描画方式（自前 SVG、外部ライブラリ不使用）

- 出荷トレンドアプリ（`shipment_trend`）が既に自前 SVG 描画を採用しているため、視覚的な一貫性のためこれに合わせた。ただしコンテキスト境界のルール上、コードは共有せず独自実装した。
- 大きな懸念はないが、デザインの好み次第で変えられる点として記載。

### 7. JS の照合単位が仕入先未解決の行を 1 つにまとめている疑い（2026/09/17 追記）

- Python 実装（05 第 2 段階）では組が空の行を連結しないが、04 の JS 実装（`buildReconciliationUnits`）は空の組を `P:|` キーで連結する。実データで JS は 25 単位・144 行、Python は 24 単位・75 行となり、差の約 70 行は仕入先未解決の行が JS 側で 1 単位にまとまっているためと考えられる。
- 影響: 該当行の詳細ダイアログの推定在庫推移で、無関係な品番の在庫・出荷が合算されている可能性がある。
- **確認したいこと**: JS 側も空の組を連結しない規則に揃えてよいか（design §3.2 では JS を当面残すとしている）。

### 5. 未push（develop へのコミットのみ）

- 実装後、develop ブランチへローカルコミットまでは行うが、**origin への push・master への反映は行わない**（このセッションの一貫した方針：pushは都度「push して」の明示指示を受けてから実行）。
- **確認したいこと**: 内容を確認後、「push して」と言っていただければ develop へ反映する。

---

## 自己承認したレビュー内容（参考・変更不要なら読み飛ばして良い）

- 要件定義書 L1/L2/L3 相当の自己レビュー: 用語（V-216 出荷推移を新設）・スコープ・異常系（出荷実績なし、既存スナップショット互換）を確認。NG 相当の指摘なし。
- 設計書レビュー: Clean Architecture のレイヤー配置、コンテキスト境界（`shipment_trend` への直接importをしない）を確認。
- 詳細は各文書末尾の「レビュー履歴」セクションを参照。
