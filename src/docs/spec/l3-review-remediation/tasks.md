# L3レビュー指摘の是正 タスクリスト

文書ID: TASK-L3REV-2026-001
作成日: 2026-08-07
更新日: 2026-08-07
対応文書:
- [検収書比較_機能仕様書.md](../../../application/receipt_comparison/docs/検収書比較_機能仕様書.md)
- [在庫発注アラート_機能仕様書.md](../../../application/inventory_order_alert/docs/在庫発注アラート_機能仕様書.md)
- [資産棚卸結果_機能仕様書.md](../../../application/asset_inventory/docs/資産棚卸結果_機能仕様書.md)
- [出荷トレンド一覧_機能仕様書.md](../../../application/shipment_trend/docs/出荷トレンド一覧_機能仕様書.md)
- [5年9組_機能仕様書.md](../../../application/gonenkukumi/docs/5年9組_機能仕様書.md)
- [L1レビュー指摘の是正 タスクリスト](../l1-review-remediation/tasks.md)（TASK-L1REV-2026-001）

本書は 2026-08-05 に実施した要件定義書 L3レビュー（妥当性・網羅性）の是正タスクを管理する。
指摘は 5 文書合計で **NG 7件 / 警告 12件**（実体は L3-1〜L3-19 の 19 件。L3-4・L3-9・L3-10・L3-15 は複数文書に重複計上）。

---

## 状況チェックシート

| 項目 | 内容 |
|------|------|
| 対象 | 5アプリの機能仕様書 + 実装（検収書比較・在庫発注アラート・5年9組） + テスト |
| レビュー結果 | 総合判定: 5年9組 **PASS** / 検収書比較 CONDITIONAL PASS / 他3文書 FAIL |
| 承認済み方針（2026-08-07） | ①L3-6: 制約の意図を「業務テーブルを作らない」に改め、セッションは技術的一時領域として適用範囲外と明記（実装変更なし） ②L3-2: 開始日 > 終了日 は画面・CSV ともエラー表示に統一（暗黙スワップを廃止） ③L3-7: 一覧への集計基準日の常時表示を要件化（鮮度警告バナーは追加しない） ④実装まで踏み込む警告: L3-4・L3-13・L3-19 の3件 |
| SDDフェーズ | 運用中文書の是正（Phase 2 相当の手戻り。実装是正を伴う） |
| 本是正の対象外 | 指摘 L3-10（`src/docs/strategic_design.md` の新設）は5文書横断かつ独立した成果物のため **別課題** とする。本書では各文書に課題として記載するに留める |

## 凡例

| 記号 | 意味 |
|------|------|
| [ ] | 未着手 |
| [~] | 作業中 |
| [x] | 完了 |
| [-] | 対象外・保留 |

---

## Phase A: NG指摘の是正 — 機能仕様書（5ファイル）

| ID | 状態 | 対象ファイル | 是正内容 | 対応指摘 |
|----|------|-------------|---------|---------|
| A-1 | [ ] | `application/inventory_order_alert/docs/在庫発注アラート_機能仕様書.md` | §12・REQ-NF-006 の Oracle 接続参照先を `application/gonenkukumi/infrastructure/oracle/client.py` → **`application/sales/infrastructure/oracle/client.py`** に訂正。「読取接続プールを5年9組と共用する」→「共有 Oracle ACL `application/sales/` を経由する（検収書比較・出荷トレンド一覧と共用）」に改める。実装照合済み（`summary_queries.py` / `summary_aggregation.py` が `application.sales` を import、`gonenkukumi` への import は存在しない） | L3-1（NG） |
| A-2 | [ ] | `application/asset_inventory/docs/資産棚卸結果_機能仕様書.md` | §3.1（161行）・§5.5（250行）の「具体的な部品名は desknet's 設定に合わせ実装時に `record_mapper` で固定する」「初版ではサンプル運用データから〜特定し、仕様書改訂履歴に記載する」を削除し、**確定済みの部品名を転記**（`domain/repositories/ports.py` の `SITE_FIELDS` = `データID` / `管理部門コード` / `拠点名`）。存在しない `record_mapper` の参照を実在パスへ訂正。§4.1.3a のフィルタ部品も `拠点名` で確定させる | L3-5（NG） |
| A-3 | [ ] | `application/asset_inventory/docs/資産棚卸結果_機能仕様書.md` | REQ-NF-006・§2.2「データの正」・§2.4 スコープ外#3 の制約文言を「**業務用テーブル（マイグレーションを伴う業務データの永続化）を作らない**」に限定し、**Django セッション等の技術的一時領域は適用範囲外**と明記。あわせて突合結果スナップショットの保持先が `django_session`（PostgreSQL）である事実を §3 に記載し、認証セッション終了で破棄されることを明示 | L3-6（NG） |
| A-4 | [ ] | `application/shipment_trend/docs/出荷トレンド一覧_機能仕様書.md` | §7.4 に **エラー応答表（400 / 401 / 403 / 404）を新設**し、実装 `use_cases/save_baseline_year.py` の 6 検証（custCode/itemCd 未指定・baselineYear 非整数・集計データなし・集計基準日なし・該当行なし・候補外の年）を要件として明文化。REQ-F-010 に異常系シナリオを追記 | L3-3（NG） |
| A-5 | [ ] | `application/shipment_trend/docs/出荷トレンド一覧_機能仕様書.md` | §5.1・§7.1 に「**一覧上部に集計基準日を常時表示する**」を要件化（REQ-F を採番）。実装は既に表示済み（`use_cases/list_page.py` 154-158行 `as_of_label` → `templates/shipment_trend/list.html` 24-25行「〜時点の集計結果」）のため**コード変更なし**。集計データが無く `as_of_label` が空になる場合の表示も併せて定義。§2.3 スコープ外#5 に「鮮度の自動検知・警告バナーは行わない。集計基準日の常時表示で代替する」と根拠を明記 | L3-7（NG） |
| A-6 | [ ] | `application/receipt_comparison/docs/検収書比較_機能仕様書.md` | §4.1.1・§10 に「**開始日 > 終了日 の場合はエラーメッセージ『開始日は終了日以前を指定してください』を表示し、比較を行わない（画面・CSV 共通）**」を追加。暗黙スワップを廃止する旨を明記（B-1 と連動） | L3-2（NG） |
| A-7 | [ ] | `application/inventory_order_alert/docs/在庫発注アラート_機能仕様書.md` | §3「システム境界・前提」および §7.2 に **SLIMS 取込の排他方針** を追加（取込中の同時実行は排他エラー「別のユーザーが取込中です」を返す）。§10 のエラー一覧に対応行を追加（B-2 と連動） | L3-4（NG） |

## Phase B: 実装の是正（承認済みスコープ）

| ID | 状態 | 対象ファイル | 変更内容 | 対応指摘 |
|----|------|-------------|---------|---------|
| B-1 | [ ] | `application/receipt_comparison/interfaces/views.py` | 117-118行の暗黙スワップ（`if start_date > end_date: start_date, end_date = end_date, start_date`）を**削除**し、比較画面・CSV 出力（280-281行付近）の**両方で検証エラー**を返す。エラーメッセージは既存のエラー表示機構に載せる。**削除対象: 117-118行の2行** | L3-2（NG） |
| B-2 | [ ] | `application/inventory_order_alert/` 配下（`interfaces/views.py` / `use_cases/import_stock.py` / `infrastructure/persistence/slims_stock_repository.py`） | SLIMS 取込の**排他制御**を追加。`slims_stock_repository.py` 21行の `SlimsStockSnapshot.objects.all().delete()` → INSERT が同一リクエスト内で全件差し替えを行うため、取込全体を**トランザクション＋ DB ロック**で保護し、競合時は排他エラーを返す。レイヤー配置は Clean Architecture に従い、ロック取得は infrastructure、判定・エラー通知は use_cases に置く | L3-4（NG） |
| B-3 | [ ] | `application/receipt_comparison/interfaces/views.py` ほか | 受領書ファイルの**サイズ上限・行数上限**を追加（在庫発注アラート REQ-NF-003 の 10 MB に横並び）。138-139行で `request.FILES.get("receipt_file").read()` を無検証で全読みしているため、読み込み前にサイズを検証し、超過時は 413 相当のエラーを返す | L3-13（警告） |
| B-4 | [ ] | `application/inventory_order_alert/interfaces/views.py` ほか | **【追加検出】** 在庫発注アラートの REQ-NF-003「最大ファイルサイズ 10 MB」・§8「413（ファイルサイズ超過）」が **実装に存在しない**（`views.py` 30-31行で `uploaded.read()` を無検証実行。`config/settings/` に `DATA_UPLOAD_MAX_MEMORY_SIZE` / `FILE_UPLOAD_MAX_MEMORY_SIZE` の設定もなく、Django 既定ではファイルアップロードにサイズ上限がかからない）。L3-13 が「在庫発注アラートは 10 MB・413 を明記しており横並びが取れていない」を根拠とする以上、**参照元自体を実装しないと横並びが成立しない**ため本タスクに含める | 追加検出（L3-13 関連） |
| B-5 | [ ] | `application/gonenkukumi/` 配下（`models.py` / 検索履歴の保存経路） | 検索履歴 `GonenKukumiSearchHistory` の**保持方針**を実装（保持上限＝ユーザーあたり件数上限、または保持期間による削除）。現状は `ordering` と index のみで削除機構がなく、ユーザー数 × 検索回数で無制限に増加する。保存時に上限超過分を削除する方式を採る（バッチはスコープ外のため） | L3-19（警告） |

## Phase C: 警告指摘の文書化（実装を伴わないもの）

| ID | 状態 | 対象ファイル | 記載内容 | 対応指摘 |
|----|------|-------------|---------|---------|
| C-1 | [ ] | `application/gonenkukumi/docs/5年9組_機能仕様書.md` | §5.1 に検索履歴の保持上限・削除方針を要件化（B-5 と連動）。§2.4 スコープ外に定期削除バッチを明記 | L3-19 |
| C-2 | [ ] | `application/receipt_comparison/docs/検収書比較_機能仕様書.md` | ①§5・REQ-NF-003 にファイルサイズ・行数上限と超過時の 413 を明記（B-3 と連動） ②§4.1.1・§8 に同時実行時の振る舞い（後勝ち／最終更新者を保持）を追加 ③§7.3 に未完了データ引き継ぎの**遡及期間の上限**と打ち切り条件を追加 ④§2.3 に旧 MySQL システムからの**既存比較結果・得意先設定の移行方針**を追加 | L3-13, L3-4, L3-12, L3-16 |
| C-3 | [ ] | `application/asset_inventory/docs/資産棚卸結果_機能仕様書.md` | ①§4.1.1 手順3・§7.4 に **4 本の並列取得で一部だけ失敗した場合**の振る舞いを追加（拠点マスタのみ失敗時は §5.5 のフォールバックで継続し警告を併記。§7.4 の 502 一律を見直す） ②§4.1.1 手順8・REQ-NF-003 に突合結果の**行数上限と超過時の振る舞い**を追加（セッション保存量・ページ埋め込み JSON 双方） ③§5.1 に資産番号が `L` 一文字／空文字となる場合の突合キーの扱いを追加 | L3-14, L3-15, 異常系提案#2 |
| C-4 | [ ] | `application/shipment_trend/docs/出荷トレンド一覧_機能仕様書.md` | ①§2.1「背景と目的」に**現状の課題**（現在どう判断していて何に困っているか）を補記 ②§5.5・§5.6 に同時実行（データ更新の同時実行・比較基準年の同時変更）の振る舞いを追加 ③§7.1.1 にブラウザ内ローカル処理の**行数上限と超過時の振る舞い**を追加 ④§7.5 に列名改称（得意先品番 → 内作品番）の**後方互換方針**を明記し、L1 是正タスクの「要確認」を決着させる | L3-8, L3-4, L3-15, L3-17 |
| C-5 | [ ] | `application/inventory_order_alert/docs/在庫発注アラート_機能仕様書.md` | ①§13 Q5 の SLIMS **取込頻度**を確定させ、`stockStaleDays` 既定 7 日の根拠を明記 ②REQ-NF-006・§7.2 に取込時 Oracle 全件集計が**共有 Oracle 接続を使う他アプリへ与える影響**と対処方針を追加 ③§4.1.1・§10.5 に Oracle 集計タイムアウト時の**前回スナップショット保持**の有無を一意に読める形で明記 | L3-11, L3-18, 異常系提案#2 |
| C-6 | [ ] | 4アプリの機能仕様書（5年9組を除く） | §2.2 に**目的の達成度を測る指標・受入基準**を追加。5年9組 §10.1「受入チェックリスト（C-01〜C-06）」を雛形とし、既存の要件から受入基準を起草する（業務目標値を伴う指標はユーザー確認を要するため、確認事項として明示する） | L3-9 |
| C-7 | [ ] | 5アプリの機能仕様書 | 指摘 L3-10（`src/docs/strategic_design.md` 不在）を**別課題**として各文書に記載する（本是正の対象外。BMC・サブドメイン分類との照合は strategic_design.md 新設後に再実施） | L3-10 |

## Phase D: テスト

| ID | 状態 | 対象ファイル | 内容 |
|----|------|-------------|------|
| D-1 | [ ] | `application/receipt_comparison/tests/` | 開始日 > 終了日 のとき、比較画面・CSV 出力の**両方**でエラーとなることを検証（B-1）。既存のスワップ前提のテストがあれば追随修正する |
| D-2 | [ ] | `application/inventory_order_alert/tests/` | SLIMS 取込の排他制御（競合時に排他エラー・スナップショットが部分破壊されない）を検証（B-2） |
| D-3 | [ ] | `application/receipt_comparison/tests/` / `application/inventory_order_alert/tests/` | ファイルサイズ上限超過時のエラー応答を検証（B-3・B-4） |
| D-4 | [ ] | `application/gonenkukumi/tests/` | 検索履歴の保持上限を超えた際に古い行が削除されることを検証（B-5） |
| D-5 | [ ] | 5アプリの `docs/*_テスト仕様書.md` | 上記の異常系ケースを追記 |
| D-6 | [ ] | — | `pytest` 全緑（現行 963 passed）と `manage.py check` を確認。`config/tests/test_clean_architecture.py` のレイヤー検証も含む |

## Phase E: レビュー履歴・改訂履歴

| ID | 状態 | 対象ファイル | 内容 |
|----|------|-------------|------|
| E-1 | [ ] | 5アプリの機能仕様書 | L3レビュー節の指摘表「対応」列を **未対応 → 是正済み（本書 A-n / B-n 参照）** に更新。未解決件数・次のアクションを更新 |
| E-2 | [ ] | 5アプリの機能仕様書 | 改訂履歴に本是正の版を追加し、更新日を 2026-08-07 に更新 |
| E-3 | [ ] | `application/asset_inventory/docs/ubiquitous_language.md` | 41行の「具体的な部品名は desknet's 設定に合わせ `record_mapper` で固定する」を確定済みの部品名に訂正（A-2 と連動） |

---

## 影響範囲

| 区分 | 影響 |
|------|------|
| ユーザー可視 | ①**検収書比較**: 開始日 > 終了日 の入力が暗黙スワップからエラー表示に変わる（**既存の操作感が変わる唯一の変更**） ②**検収書比較・在庫発注アラート**: ファイルサイズ超過時にエラーとなる（従来は無制限に読み込み） ③**在庫発注アラート**: 取込の同時実行時に排他エラーが出る |
| データ | **5年9組の検索履歴に削除が発生**（保持上限超過分）。他はスキーマ変更なし。B-2・B-5 でマイグレーションが必要になる場合は事前に提示する |
| 外部連携 | **なし**（Oracle SQL・SLIMS CSV 取込・desknet's API の項目は変更しない。A-1 は仕様書の参照先表記の訂正のみで、実装は元から `application/sales/` を使用） |
| 後方互換 | 出荷トレンド一覧の CSV ヘッダー後方互換（L3-17）は **C-4 で方針を明記して決着させる**。ヘッダー名そのものは変更しない |
| 削除を伴う変更 | ①`receipt_comparison/interfaces/views.py` 117-118行の暗黙スワップ 2 行 ②資産棚卸結果 仕様書の `record_mapper` 記述 3 箇所＋UL 1 箇所 ③出荷トレンド一覧 §2.3 スコープ外#5 の記述差し替え ④5年9組 検索履歴の保持上限超過分（実行時のデータ削除） |
| 対象外 | 指摘 L3-10（`src/docs/strategic_design.md` の新設）。5文書横断の独立成果物として別途扱う |

## 確認事項（実施中にユーザー判断を要する点）

| # | 対象 | 内容 |
|---|------|------|
| 1 | B-3・B-4 | ファイルサイズ上限の具体値。仕様書の 10 MB を採用してよいか（行数上限を併用するかも含む） |
| 2 | B-5 | 検索履歴の保持方針。件数上限（ユーザーあたり N 件）か保持期間（N 日）か、およびその値 |
| 3 | C-5 ① | SLIMS 取込頻度（§13 Q5 が未定のまま）。`stockStaleDays` 既定 7 日の根拠となる運用前提 |
| 4 | C-6 | 受入基準の目標値。業務目標を伴う指標はユーザー確認を要する |
| 5 | C-2 ③④ | 未完了データの遡及期間の上限、および旧 MySQL システムからの移行方針 |

## タスク実行レポート

| 日付 | タスクID | 結果 | 備考 |
|------|---------|------|------|
| 2026-08-07 | A-1・A-7 | 完了 | 在庫発注アラート: Oracle 参照先を共有 ACL `application/sales/` に訂正。取込の排他方針を §3・§7.2・§8.2・§10.4 に追加 |
| 2026-08-07 | A-2・A-3 | 完了 | 資産棚卸結果: `record_mapper` を確定部品名（`SITE_FIELDS`）に置換（仕様書3箇所＋UL 1箇所）。永続化制約を「業務用テーブルを作らない」に限定し §3 に「突合結果の保持先」を新設 |
| 2026-08-07 | A-4・A-5 | 完了 | 出荷トレンド一覧: §7.4.1 エラー応答表を新設（E-01〜E-07）。REQ-F-013 として集計基準日の常時表示を要件化（実装は既存のため変更なし） |
| 2026-08-07 | A-6・B-1 | 完了 | 検収書比較: 日付逆転を画面・CSV 双方でエラー化。`interfaces/views.py` の暗黙スワップ 2 行を削除し、domain に `date_range_error` を追加 |
| 2026-08-07 | B-2 | 完了 | SLIMS 取込の排他制御を実装。`infrastructure/persistence/import_lock.py`（PostgreSQL トランザクションスコープのアドバイザリロック）と `domain/value_objects/errors.py` を新設 |
| 2026-08-07 | B-3・B-4 | **対象外** | **上限を設けない方針をユーザーが決定**（2026-08-07）。実装は行わず、在庫発注アラート REQ-NF-003 の「10 MB・413」（**未実装だった**）を撤回し、検収書比較 REQ-NF-003 にも同方針を明記 |
| 2026-08-07 | B-5・C-1 | 完了 | 5年9組: 検索履歴をユーザーあたり最新 100 件に保持（保存時に超過分を削除）。§5.1.1 に要件化 |
| 2026-08-07 | C-2〜C-7 | 完了 | 警告 12 件を 5 文書に文書化。受入基準（C-01〜C-06）を 4 文書に新設。L3-10 は横断課題として §1 直後に明記 |
| 2026-08-07 | D-1〜D-6 | 完了 | テスト 3 ファイル・10 件を追加（日付逆転 4・取込排他 3・履歴保持 3）。**pytest 973 passed**（従前 963）／`manage.py check` 問題なし |
| 2026-08-07 | E-1〜E-3 | 完了 | 5 文書のレビュー履歴「対応」列を是正済みに更新。改訂履歴に版を追加（資産棚卸結果 2.65／在庫発注アラート 3.31／検収書比較 3.12／出荷トレンド一覧 1.51／5年9組 3.4）。更新日を 2026-08-07 に更新 |
| 2026-08-19 | F-1 | 完了 | **L3 再レビュー**を 5 文書で実施（指摘の記述を信用せず仕様と実装を照合）。前回 NG 7 件の反映を全件確認。新規指摘 2 件（L3R-1 資産棚卸結果／L3R-2 在庫発注アラート）を各文書のレビュー履歴に追記 |
| 2026-08-19 | F-2 | 完了 | **L3R-2 の是正**（在庫発注アラート）。§7.2「排他・整合性」の「409 を返し」を「取込を行わず一覧画面にエラーメッセージを表示する」に訂正し、§3・§8.2・実装と一致させた（改訂 3.32） |
| 2026-08-19 | F-3 | 完了 | **L3R-1 の是正**（資産棚卸結果）。§7.4.1 に実装を追随させた（拠点マスタのみ失敗時は一覧・CSV を継続し警告を表示。縮退結果はスナップショットに保存しない）。あわせて §7.4.1 の事実誤認 2 点と、会社マスタが未取得である事実を REQ-F-002・§4.1.1・§5.5・§6 に反映（改訂 2.66）。変更 7 ファイル（`desknet_data.py` / `reconcile_data.py` / `ports.py` / `list_page.py`(UC) / `export_csv.py` / `views.py` / `list.html` ＋ `app.css`） |
| 2026-08-19 | F-4 | 完了 | 残課題 #2「API は 401」を調査のうえ訂正。**5年9組は `api_login_required` が実際に JSON 401 を返すため記述は正しく、対象外**。在庫発注アラート（§7.1・§8.1・§8.11）・資産棚卸結果（§7.1・§7.4）を 302 に訂正。検収書比較には該当記述なし |
| 2026-08-19 | F-5 | 完了 | テスト 5 件を追加（TC_AIV_UC_030〜034: 拠点マスタ失敗時の警告・フィルタ候補・非キャッシュ・資産データ失敗時のエラー・CSV 継続）。**pytest 978 passed**（従前 973）／`manage.py check` 問題なし |

## 実施中に検出した追加の不整合（L3 指摘外）

いずれも「仕様書に書かれているが実装にない／実装と違う」もので、本是正で **仕様書側を実態に合わせて訂正** した。

| # | 対象 | 内容 | 対応 |
|---|------|------|------|
| 1 | 在庫発注アラート §8.2 | `POST /api/inventory-order-alert/stock-import` という **API エンドポイントが存在しない**（`interfaces/urls.py` に定義なし）。取込は一覧画面 `POST /app/production/inventory-order-alert` で受け付けている | 仕様書を実態に合わせて全面的に書き替えた |
| 2 | 在庫発注アラート REQ-NF-003・§8 | 「最大ファイルサイズ 10 MB」「413（ファイルサイズ超過）」が **実装に存在しない**（`views.py` は無検証で `read()`、`DATA_UPLOAD_MAX_MEMORY_SIZE` 等の設定もなし） | 上限を設けない方針の決定に伴い記述を撤回 |
| 3 | 出荷トレンド一覧 §3 | 「API は 401」とあるが、実装は `@login_required` による **302 リダイレクト**（401 を返すミドルウェアは存在しない） | §3・§7.4.1 を実装どおりに訂正。**他 4 文書にも同じ記述が残っている**（下記「残課題」） |
| 4 | 出荷トレンド一覧 §5.5 | データ更新の実行中は最新の更新レコードに対応するスナップショットが未作成のため、**一覧が空になる**。また同時実行時は「後から開始した実行」が採用され、完了順ではない | §5.5 に現行の振る舞いとして明記（実装は変更せず） |
| 5 | 資産棚卸結果 §10 改訂履歴 | 版 2.64 が 2.63 より前に置かれ、版順が乱れていた | 2.62 → 2.63 → 2.64 の順に整列 |
| 6 | 資産棚卸結果 REQ-F-002・§4.1.1・§5.5・§6・§7.4.1 | 「**会社マスタ**を取得する」と 5 箇所に書かれているが、実装は `company_app_id` を解析するだけで **API 取得も画面表示も行っていない**（`fetch_reconcile_source_data` は資産・棚卸・拠点マスタの 3 本のみ） | 仕様書を実態に合わせ「初版では取得も表示も行わない」と訂正（2026-08-19） |
| 7 | 資産棚卸結果 §7.4.1 | 「拠点マスタのみ失敗 → 拠点フィルタと拠点名の表示は利用できません／候補は空にする」とあるが、**拠点名・拠点フィルタ候補は資産データの `管理部門名称` 由来**で拠点マスタに依存しない（拠点マスタは工場変化判定のみに使用） | 仕様書を訂正し、実装は「警告のみ表示・候補は維持」とした（2026-08-19） |
| 8 | 在庫発注アラート §8.1・§8.4・§8.9・§8.10・§8.11 | 記載された API エンドポイントが `interfaces/urls.py` に存在しない（**未実装**）。一覧は Oracle スナップショットからサーバサイド描画するため画面動作に影響はない | **未対応**。在庫発注アラート仕様書の再レビュー節に別課題として記録（下記 残課題 #7） |

## 残課題

| # | 内容 | 扱い |
|---|------|------|
| 1 | 指摘 L3-10: `src/docs/strategic_design.md` の新設と、共有 Oracle ACL `application/sales/` の所有コンテキスト定義 | **別課題**。5 文書に横断課題として記載済み。作成後に L3 観点2 を再レビューする |
| 2 | 「API は 401」の記述が 5年9組・検収書比較・資産棚卸結果・在庫発注アラートの §3 に残存（実装は 302 リダイレクト） | **解消（2026-08-19）**。調査の結果、**5年9組は `interfaces/views.py` の `api_login_required` が実際に JSON 401 を返すため記述が正しく対象外**、検収書比較には該当記述なし。資産棚卸結果・在庫発注アラートを 302 に訂正した |
| 3 | 指摘 L3-11: SLIMS 取込頻度（`stockStaleDays` 既定 7 日の根拠） | **業務確認待ち**。§13 Q5 に依存関係と暫定運用を記載済み |
| 4 | 指摘 L3-12: 検収書比較の未完了データ遡及期間の上限 | **未決着**。論点を §7.3 に記載済み。運用実績を見てから確定する |
| 5 | 指摘 L3-16: 検収書比較の旧 MySQL システムからのデータ移行方針 | **未決着**。論点を §2.3 に記載済み。**本番切替の前に確定させる必要がある** |
| 6 | 受入基準の定量指標（5 文書共通） | **未確定**。現行運用の実測値が無く基準値を置けないため、運用開始後に定める旨を各文書に明記 |
| 7 | 在庫発注アラート §8 に未実装 API が 5 節ある（`summary` / `stock-locations` / `vendors` / 設定 API / `dashboard-summary`） | **未対応**。実装・削除・「未実装」明記のいずれかを次回レビューで判断する |
