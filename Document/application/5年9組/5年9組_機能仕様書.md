# 5年9組 機能仕様書（実装反映版）

本書は、現行実装（Web/Excel 出力/Oracle 参照/履歴DB）を解析して、**ベンダーへ提示すれば同等のものが実装できる**粒度で要件を固定する。

## 1. 文書情報

| 項目 | 内容 |
|------|------|
| プログラム名 | **5年9組** |
| 親システム | Core Data Integration Portal（基幹データ連携ポータル） |
| 文書の目的 | Excel マクロ相当の参照機能を Web 化し、検索・結果表示・履歴・Excel 出力を提供する。**現行実装準拠**で仕様を固定する |
| 想定読者 | ベンダー開発者、業務担当、プロジェクト関係者 |
| 関連実装 | `src/domains/gonenkukumi/`（`cust-headline.ts`・`year-month-nav.ts` 等）、`src/infrastructure/oracle/gonenkukumi/run-search.ts`、`list-customers.ts`、`src/components/gonenkukumi/`、`POST /api/gonenkukumi/oracle-result`、`excel-export.ts` |

---

## 2. システム境界・前提

| 区分 | 内容 |
|------|------|
| 基幹 Oracle | **参照のみ**（SELECT）。接続はサーバ側のみ。 |
| ポータル PostgreSQL | 検索履歴のみ保存（結果スナップショットは保存しない）。 |
| 認証 | Auth.js（ログイン必須）。未ログインは `/login` へ遷移、API は 401。 |

---

## 3. 画面（ルーティング）

| 画面 ID | 画面 | ルート | 概要 |
|---|---|---|---|
| SCR-01 | 検索画面 | `/app/production/five-year-nine` | 検索条件入力、検索実行、履歴（直近20件・表形式）表示 |
| SCR-02 | 結果画面 | `/app/production/five-year-nine/result` | クエリ（検索条件）で Oracle 再検索→結果表示。履歴 ID は用いない |

### 3.1 検索画面（SCR-01）

#### 3.1.1 入力項目（実装固定）

- **画面レイアウト順**（上から）: 得意先コード → 得意先品目 → 品目任意変換値 → **検索年月と対象日付を同一行**（狭い幅では縦積み）→ **検索ボタン** → **エラー表示**（発生時のみ、ボタンの直下）。

| 項目 | キー | 型/形式 | 必須 | 既定値 | 備考 |
|---|---|---|---|---|---|
| 得意先コード | `custCode` | string | 必須 | なし | Oracle `M_CUST` から `GET /api/gonenkukumi/customers` で一覧取得し、**コンボボックス**（入力・▼一覧・キーボード）で指定。一覧取得失敗時も**手入力可**。候補は **数字3桁コードのみ**。候補表示は **コード → 名称** |
| 得意先品目 | `custItem` | string | 必須 | なし | **得意先コード未入力では入力不可（disabled）**。得意先コードが変わったら自動でクリア（履歴反映時はクリアしない）。オートコンプリートは §3.1.4 |
| 品目任意変換値 | `optionChange` | string | 任意 | `*` | 空のとき `*` 扱い |
| 検索年月 | `yearMonth` | `yyyy/mm` | 必須 | 当月 | `schemas.ts` バリデーション |
| 対象日付 | `asOfDate` | `yyyy/mm/dd` | 必須 | 当日 | UI は `type=date`（`yyyy-MM-dd`）に変換 |

#### 3.1.2 操作（実装固定）

- **検索ボタン**: `/api/gonenkukumi/search` に POST。成功時、**同一の検索条件をクエリ文字列に載せて**新規タブで `SCR-02`（`/app/production/five-year-nine/result?...`）を開く。
- **履歴クリック**: 履歴をクリックすると、検索条件にその内容を反映する（即時検索は行わない）。対象日付は履歴からは変更しない（画面左の入力のまま）。

#### 3.1.3 履歴表示（実装固定）

- SSR で **条件セット（得意先コード/得意先品目/品目任意変換値/検索年月）ごとに最新1件**を取得し、最大**20件**を初期表示する（同一条件は検索日時が新しいもののみ表示）。
- 画面上は **表（テーブル）** で表示する。列は **得意先コード／得意先品目／品目任意変換値／検索年月／検索日時**（対象日付列は置かない。行クリックでも対象日付は変更しない。スラッシュ等で1行に連結したテキスト表示もしない）。**lg 以上では各列を均等幅（各 20%）**とする。
- 履歴一覧は **約8〜9行分（max-height 20rem）で縦スクロール**し、テーブルヘッダ（列名行）は **スクロールしても固定（sticky）**する。
- API `/api/gonenkukumi/history` も提供（最大50件）。

#### 3.1.4 得意先品目オートコンプリート（実装固定）

- **プリフェッチ方式**: 得意先コードが確定した瞬間に `GET /api/gonenkukumi/cust-items?custCode=&asOfDate=` を 1 回呼び、当該得意先の「得意先品目」全件をクライアントメモリにキャッシュする。
- **入力ごとの候補表示**: 入力 **1文字目から**、キャッシュを **先頭一致（startsWith）**で絞り込み、候補を表示する（入力のたびに API は呼ばない）。
- **候補表示上限**: UI 表示は最大 **1000件**（1社1000件想定）。
- **取得中の扱い**: キャッシュ取得完了前は「一致する得意先品目がありません。」は表示しない。
- **一致なしメッセージ**: 取得完了後に候補が 0 件のとき、赤文字で **「一致する得意先品目がありません。」** を表示する。

### 3.2 結果画面（SCR-02）

- **クエリパラメータ**（`custCode`, `custItem`, `optionChange`, `yearMonth`, `asOfDate`）を検証し、**Oracle へ再問い合わせ**して描画する（スナップショット保持なし）。履歴テーブルは参照しない。
- 検索条件ブロックの **得意先**は、Oracle 結果の略称名とクエリのコードから **`得意先名(得意先コード)`** で表示する（名が取れないときはコードのみ）。
- ブラウザタイトルは **`得意先名(得意先コード)：得意先品目`** とする（`generateMetadata` とページ本体で同一 Oracle 呼び出しを `cache` で共有）。
- **結果の縦積み（階層ブロック単位）**: 画面は **得意先 出荷**／**仕入先 入荷（BOM 階層ごと）** のブロックに分割する。**Web 結果画面では複数得意先の「ＴＯＴＡＬ」合計表は表示しない**（Oracle 取得データおよび Excel 出力側の仕様は別途 §6・§8）。**「前月を追加／次月を追加」は検索条件ブロックにのみ置き、押下すると表示中の全ブロック（得意先・全仕入階層）に同じ暦月の Oracle 結果がまとめて追加**される（新規タブは開かない）。追加の基準は「画面全体で読み込み済みの月のうち最も古い月の前月」「最も新しい月の次月」。同一月の二重追加は不可。検索画面と同じ範囲（**2022/01**〜**対象日付から6か月後の暦月**）外へは出せず、該当ボタンは無効。追加データは **`POST /api/gonenkukumi/oracle-result`** で取得する（履歴テーブルには記録しない）。**各月は折りたたみ可能**（`<details>` 相当）。`<summary>` には **検索年月** と **その月の代表行**（得意先ブロックは先頭得意先の見出し相当、仕入ブロックは該当階層の先頭仕入の見出し相当）を載せ、**当該行をクリックして開閉**する。階層ブロック単位の別帯見出しや「クリックで展開」等の補助文は置かない。
- **日次列（Web 表示）**: 表の日次列は **常に 31 列**（1日〜31日）。当月に存在しない日付列は **空白**とし、**背景はその表ブロックの階層テーマ色**（見出しは `cssLightBg`、データ部は `cssVeryLightBg` 相当の系統色。7.1／7.2 の色割当）で示す。
- Excel 出力ボタン「Excelで出力」を表示し、**URL の検索年月（基準月）**に対応する Oracle 結果から Excel を生成してダウンロードする（追加表示した月のみを出力する操作は行わない）。

---

## 4. API 仕様（Next.js Route Handlers）

### 4.1 `POST /api/gonenkukumi/search`

- **認証**: 必須（未ログインは 401）
- **入力**: JSON

```json
{
  "custCode": "string",
  "custItem": "string",
  "optionChange": "*",
  "yearMonth": "yyyy/mm",
  "asOfDate": "yyyy/mm/dd"
}
```

- **バリデーション**: `src/domains/gonenkukumi/schemas.ts`（Zod）
- **処理**:
  - Oracle 一括検索 `runGonenKukumiOracleSearch`
  - 成功時のみ履歴 INSERT（**対象日付・`custName` は PostgreSQL に保存しない**）
- **成功応答**:

```json
{ "ok": true }
```

- **失敗応答（代表）**:
  - 400: 入力エラー（details 付き）
  - 404: 内作品番未解決（`NAISAK_NOT_FOUND`）
  - 502: Oracle エラー
  - 503: Oracle 未設定（`ORACLE_NOT_CONFIGURED`）

### 4.2 `GET /api/gonenkukumi/customers`

- **認証**: 必須（未ログインは 401）
- **処理**: Oracle `M_CUST` から得意先コード・略称名を取得（最大 500 行）。**数字3桁コードのみ**を対象にする。未設定時は空配列。
- **成功応答**:

```json
{
  "items": [{ "custCode": "string", "custName": "string" }]
}
```

- 取得例外時は HTTP 200 で `items: []` と `loadError`（文字列）を返し、画面は手入力にフォールバックする。

### 4.3 `GET /api/gonenkukumi/history`

- **認証**: 必須（未ログインは 401）
- **応答**: 最大50件、新しい順

```json
{
  "items": [
    {
      "id": "string",
      "executedAt": "ISO",
      "custCode": "string",
      "custItem": "string",
      "yearMonth": "yyyy/mm"
    }
  ]
}
```

### 4.4 `POST /api/gonenkukumi/oracle-result`

- **認証**: 必須（未ログインは 401）
- **目的**: 結果画面で「前月／次月を追加」したときに、同一検索条件・別 `yearMonth` の Oracle 結果を JSON で返す（**履歴 INSERT は行わない**）。
- **入力**: `POST /api/gonenkukumi/search` と同じ JSON 本体（`schemas.ts`）
- **成功応答**: `{ "ok": true, "oracle": { ... } }`（`oracle` は `GonenKukumiOracleSuccess` 相当）
- **失敗応答**: `POST /api/gonenkukumi/search` と同様（400 / 404 / 502 / 503）

### 4.5 `GET /api/gonenkukumi/cust-items`

- **認証**: 必須（未ログインは 401）
- **目的**: 得意先品目のオートコンプリート用に、得意先コード確定時に **全件プリフェッチ**する。
- **入力**: `custCode`（必須）, `asOfDate`（`yyyy/mm/dd` 必須）
- **成功応答**:

```json
{ "items": ["string"] }
```

- **備考**: サーバ側で Oracle 未設定時は `items: []` を返す。

---

## 5. 検索履歴（PostgreSQL / Prisma）

### 5.1 エンティティ（実装固定）

`prisma/schema.prisma` の `GonenKukumiSearchHistory` に準拠。

| 列 | 型 | 説明 |
|---|---|---|
| id | String (cuid) | 主キー |
| userId | String | ログインユーザー |
| executedAt | DateTime | 実行日時（default now） |
| custCode | String | 入力 `custCode` |
| custItem | String | 入力 `custItem` |
| optionChange | String | 入力 `optionChange` |
| yearMonth | String | 入力 `yearMonth` |

※対象日付（`asOfDate`）は **PostgreSQL には保存しない**（検索 API の入力としてのみ使用）。

---

## 6. Oracle 参照（論理）

### 6.1 一括検索

- 入口: `runGonenKukumiOracleSearch`（`src/infrastructure/oracle/gonenkukumi/run-search.ts`）
- 取得する結果（概念）:
  - **内作品番**（`internalItemCd`）
  - **休日一覧**（`holidayDays`：1〜31 の日番号）
  - **得意先ブロック**（出荷側）
  - **TOTAL**（得意先複数時、集計データは保持。Web 画面では表示しない）
  - **仕入先ブロック**（入荷側、BOM 展開）

### 6.2 仕入先ブロックで使用する `M_PS` 列

- `LEVEL`（CONNECT BY の階層）を `kaiso` として保持する。
- `CONS_TYP` を保持し、表示上「支給区分」に変換して表示する。
  - 0: 非
  - 1: 有償支給
  - 2: 無償支給

---

## 7. 表示仕様（Web/Excel 共通）

### 7.1 セクション共通テーマ（見た目の固定）

結果表は「セクション」単位にテーマ色を割り当て、以下を**共通仕様**として適用する。

- **Web 結果画面の日次グリッド**: 日列は **31 列固定**（§3.2）。当月に無い日は **当該セクションの階層テーマ色**で空白セル化（グレー固定ではない）。Excel 出力の日列数は当月の実日数に準拠（§8）。
- **外枠**: 濃い色（テーマ色）
- **台タイトル行**: 背景=基準色、枠=やや薄い色（テーマ色）
- **日付行（区分/行計/前月残/1日…）**: 背景=基準色、枠=やや薄い色（テーマ色）
- **休日列（M_CAL）**: 背景=基準色（データ部の日次セル）
- **左の区分セル（内示受注、月初発注等）**: 背景=すごく薄い色（テーマ色）
- **その他枠（グリッド）**: `#E2E8F0` 固定

### 7.2 基準色の割当（実装固定）

- 得意先セクション: index=1
- TOTAL セクション（Excel 等）: index=2（得意先複数時のみ）。Web 結果画面では当該セクションを表示しない
- 仕入先セクション: index=2+`kaiso`（**同一階層は同一色**）

色生成のアルゴリズムは `src/domains/gonenkukumi/kaiso-color.ts` に準拠（黄金角 + HSL）。

---

## 8. Excel 出力仕様（実装固定）

### 8.1 出力方法

- 結果画面のボタン「Excelで出力」で、ブラウザ内で xlsx を生成しダウンロードする。
- 実装: `src/domains/gonenkukumi/excel-export.ts`
- 同一画面に複数月を追加表示している場合でも、**出力対象は URL の検索年月（基準月）に対応する Oracle 結果のみ**とする。

### 8.2 ブック/シート/ファイル名

- シート名: `5年9組`
- 1行目タイトル: `5年9組 検索結果`（全列結合）
- ファイル名: `{custCode}_{custItem}_{yyyyMM}_{yyyyMMddHHmmss}.xlsx`（東京時刻、禁止文字は `_` 置換）
- フォント: **MS PGothic（ＭＳ Ｐゴシック）**
- 数値: 千区切り `#,##0`

### 8.3 先頭（検索条件ブロック）

- 2〜5行目に検索条件を配置（A〜C 列のみ枠線、D 列以降は塗り無し）

### 8.4 セクション出力

各セクションは以下の順で出力する（各セクションに日付行を持つ）。

1. 台タイトル（全列結合 1 行）
2. 日付行（`区分/行計/前月残／在庫/1日…` の 1 行）
3. データ行（複数行）

### 8.5 色・罫線

7.1 の共通仕様を Excel に適用する（外枠・台タイトル・日付行・休日列・左区分セル・固定グリッド色）。

---

## 9. Oracle セキュリティ要件（必須）

| No | 要件 | 内容 |
|----|------|------|
| S-01 | 専用ユーザー＋最小権限 | 読取専用、必要最小限の SELECT のみ |
| S-02 | 秘密情報の外部管理 | 接続情報はソース/Gitに含めない |
| S-03 | サーバ側接続のみ | ブラウザから Oracle へ直結しない |

---

## 10. 改訂履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 1.17 | 2026-05-08 | 得意先品目オートコンプリートをプリフェッチ方式（得意先コード確定時に全件取得→1文字目からローカル絞り込み）に変更。履歴表を縦スクロール＋ヘッダ固定。得意先コード候補を3桁のみ＋コード→名称表示に変更 |
| 1.16 | 2026-05-08 | 月ごとの折りたたみ見出しを検索年月＋得意先／仕入代表行に統一。表上の重複見出しは省略（複数行時は識別用1行のみ） |
| 1.15 | 2026-05-08 | 前月／次月追加を検索条件ブロックのみ・全階層一括追加に変更。無効日列を階層色表示に変更 |
| 1.14 | 2026-05-08 | Web 結果画面からＴＯＴＡＬ（複数得意先合計）ブロックを廃止 |
| 1.13 | 2026-05-08 | 結果画面の月追加を階層ブロック単位に変更。日次列を常時31列（無効日はグレー）、日付ヘッダーを階層色に統一 |
| 1.12 | 2026-05-08 | 結果画面の前月／次月を同一ページ内の縦積み＋折りたたみに変更。`POST /api/gonenkukumi/oracle-result` を追加（履歴なし） |
| 1.11 | 2026-05-08 | 結果画面に前月／次月ボタン（新規タブ・検索年月のみ変更、`year-month-nav.ts`） |
| 1.10 | 2026-05-08 | 検索履歴テーブルを列均等割り。結果画面の得意先表示・タブタイトルを `得意先名(得意先コード)` に統一（`cust-headline.ts`・React `cache`） |
| 1.9 | 2026-05-08 | 検索条件の並びを変更（検索年月・対象日付を同一行、エラーを検索ボタン下に） |
| 1.8 | 2026-05-08 | 得意先コードを Oracle `M_CUST` 一覧ベースのコンボボックスに変更（`GET /api/gonenkukumi/customers`） |
| 1.7 | 2026-05-08 | 検索履歴の説明文を「履歴をクリックすると、検索条件にその内容を反映します。」に統一 |
| 1.6 | 2026-05-08 | 結果画面を履歴 ID からクエリパラメータ方式へ変更。検索 API は `{ ok: true }` のみ返却。履歴テーブルから `custName` 列を削除 |
| 1.5 | 2026-05-08 | 検索履歴テーブルから対象日付列を削除（INSERT・履歴 API・SSR の distinct から除外） |
| 1.4 | 2026-05-08 | 履歴クリック時は対象日付を反映しない（既定・当日運用） |
| 1.3 | 2026-05-08 | 検索履歴の行クリックで検索条件（対象日付含む）へ反映する操作を復活 |
| 1.2 | 2026-05-08 | 検索履歴から対象日付列を廃止。履歴行クリックによる検索条件への反映を廃止 |
| 1.1 | 2026-05-08 | 検索履歴の最大表示件数を20件に変更。履歴一覧を表形式表示に変更（スラッシュ連結を廃止） |
| 1.0 | 2026-05-07 | 現行実装解析に基づき全面改稿（API/DB/色/Excel出力/CONS_TYP/KAISO を仕様化） |

