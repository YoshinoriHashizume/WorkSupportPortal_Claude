# 検収書比較 機能仕様書

本書は、旧 VB.NET WinForms **受領書比較(MARI)** を業務支援ポータル上で再実装した **検収書比較** の要件を定義する。  
受領書ファイル（完成品 CSV / 支給品 TXT）と MARI（Oracle）の出荷・売上・受入データを突合し、結果を `〇` / `△` / `×` で管理する。

旧 WinForms のメニュー・ログイン・TopLink 版は対象外とする。

## 1. 文書情報

| 項目 | 内容 |
|------|------|
| プログラム名 | **検収書比較** |
| 旧機能名 | 受領書比較(MARI) / 完成品受領チェック / 支給品受領チェック |
| 親システム | 業務支援ポータル |
| 配置メニュー | 生産管理 > 検収書比較 > **完成品** / **支給品** |
| 文書の目的 | 取込・MARI 参照・比較・保存・CSV 出力を実装可能な粒度で固定する |
| 想定読者 | 業務担当（生産管理）、ベンダー開発者、テスト担当、プロジェクト関係者 |
| 関連実装 | Django アプリ `receipt_comparison` 配下の domain / usecase / infrastructure / views / templates |
| 関連ドキュメント | [検収書比較_テスト仕様書.md](./検収書比較_テスト仕様書.md)、[5年9組_機能仕様書.md](../5年9組/5年9組_機能仕様書.md)（Oracle 前提）、[MARI_Oracle_テーブル定義.md](../5年9組/MARI_Oracle_テーブル定義.md) |

### 1.1 実装技術（ポータル共通前提）

| 区分 | 内容 |
|------|------|
| Web | Python + Django |
| 認証 | Django 認証 + desknet's NEO Login API（暫定） / Microsoft Entra ID（将来） |
| 業務データ | PostgreSQL（取引先設定・取込履歴・比較結果）。完成品/支給品で **テーブル分離** |
| 基幹参照 | python-oracledb（読取のみ）。`ORACLE_*` 環境変数 |
| セッション | 未登録比較プレビューは Django セッション（`HttpPendingComparisonStore`） |
| CSV 出力 | Shift_JIS（CP932）基本。`filename*`（RFC 5987）で日本語ファイル名 |

### 1.2 レイヤー構成（クリーンアーキテクチャ・5年9組準拠）

| レイヤー | パス | 責務 |
|----------|------|------|
| インターフェース | `views.py`, `urls.py`, `templates/` | HTTP 入出力。`composition` 経由でユースケース呼び出し |
| コンポジション | `composition.py` | ユースケース組み立て・ゲートウェイ注入 |
| ユースケース | `usecase/usecase_*.py` | ユースケース（画面・比較・登録・CSV・設定） |
| ドメイン | `domain/` | 比較ロジック・ファイル解析・区分・表示・ソート |
| インフラ | `infrastructure/oracle/`, `infrastructure/persistence/`, `infrastructure/session/` | Oracle・ORM・セッション |

**ユースケース一覧（`usecase/`）**

| ファイル | クラス | 概要 |
|----------|--------|------|
| `usecase_comparison_page.py` | `ComparisonPageUsecase` | 比較画面 SSR・保存済み結果読込・ソート |
| `usecase_compare.py` | `CompareUsecase` | ファイル取込・MARI 取得・突合・セッション保持 |
| `usecase_register.py` | `RegisterComparisonUsecase` | 未登録結果の PostgreSQL 登録 |
| `usecase_update_results.py` | `UpdateResultsUsecase` | 保存済み結果の `receipt_flag`・備考更新 |
| `usecase_export_csv.py` | `ExportCsvUsecase` | CSV ダウンロード |
| `usecase_settings.py` | `SettingsPageUsecase` / `SettingsPostUsecase` | 受入/納品場所設定画面 |

補助モジュール: `domain/comparison.py`（突合）、`domain/file_parser.py`（CSV/TXT）、`domain/comparison_type.py`（完成品/支給品）、`domain/comparison_display.py`・`domain/comparison_sort.py`（表示・ソート）、`domain/settings_labels.py`、`domain/ports.py`。ORM 振り分けは `infrastructure/persistence/model_registry.py`。MARI 取得は `infrastructure/oracle/mari_gateway.py`。未登録プレビューは `infrastructure/session/pending_comparison_store.py`。

**比較区分（`type` クエリ）**

| `type` | 内部値 | メニュー |
|--------|--------|----------|
| `finished-product` | `finished_product` | 完成品 |
| `supplied-parts` | `supplied_parts` | 支給品 |

レガシー URL `/app/production/receipt-comparison/{slug}/...` は `?type=` へリダイレクトする。

---

## 2. 背景・業務目的

### 2.1 背景

客先・取引先から受領する受領書データと、MARI（Oracle）の出荷/売上/受入実績を突合し、受領内容が基幹と一致しているかを生産管理が確認する。旧 WinForms アプリ `ProductionManegementSystem` の受領書比較を Web 化した。

### 2.2 本機能の目的

| 目的 | 内容 |
|------|------|
| 突合の効率化 | 受領ファイル取込と MARI 自動照合で `〇`/`△`/`×` を一覧化 |
| 差異の継続管理 | `〇` 以外の未完了行を次回比較にも引き継ぐ |
| マスタ管理 | 得意先ごとの受入/納品場所・支給品子取引先・品番設定 |
| 帳票出力 | 表示中結果の CSV ダウンロード（印刷は初期対象外） |

### 2.3 旧実装からの引き継ぎ

| 引き継ぐ | 引き継がない |
|----------|----------------|
| MARI 参照のみ、3項目一致で `〇` | WinForms 固有 UI、旧アプリログイン |
| 比較結果保存・CSV 出力 | MySQL（→ PostgreSQL） |
| 納品/受入場所の対象・除外 | TopLink 版、直接印刷 |
| 完成品 CSV / 支給品 TXT の別取込 | |

---

## 3. システム境界・前提

| 区分 | 内容 |
|------|------|
| 受領書ファイル | Web からアップロード（完成品 `.csv`、支給品 `.txt`） |
| MARI（Oracle） | サーバー側 **SELECT のみ** |
| PostgreSQL | 取引先・受入/納品場所設定、取込履歴、比較結果（完成品/支給品でテーブル分離） |
| 未登録プレビュー | Django **セッション**（DB 非保存） |
| 認証 | Django 認証必須 |
| 権限 | **生産管理** グループまたは管理者（§11） |
| データ鮮度 | 比較・表示の都度 Oracle 再取得（保存済み行は PostgreSQL から読込後 MARI とマージ） |

---

## 4. 画面（ルーティング）

| 画面 ID | 画面 | ルート | 概要 |
|---------|------|--------|------|
| SCR-01 | 検収書比較 | `/app/production/receipt-comparison?type=finished-product\|supplied-parts` | 比較・表示・登録・更新・CSV |
| SCR-02 | 受入/納品場所設定 | `/app/production/receipt-comparison/settings?type=...` | 得意先マスタ・場所/品番設定（**管理者のみ**） |
| — | CSV 出力 | `GET /app/production/receipt-comparison/export?type=...` | 表示中結果のダウンロード |

比較画面は **SSR フォーム POST**（`display` / `compare` / `register` / `update` アクション）で処理する。独立 REST API は設けない。

### 4.1 検収書比較画面（SCR-01）

#### 4.1.1 操作フロー

1. 得意先・比較日（開始〜終了）を指定し **表示** → PostgreSQL の保存済み結果を読込（未登録プレビューはクリア）。
2. **表示後**、比較結果エリア右上の **ファイル選択** で受領書を選ぶと **即時比較**（MARI 取得 + 突合）。結果はセッションに **未登録** 保持。
3. 問題なければ **登録** で PostgreSQL へ保存。
4. 保存済み表示中は **更新** で `receipt_flag`・備考を更新。
5. **CSV** で表示中データ（未登録含む）をダウンロード。

表示前は比較結果カードに「比較結果はありません。」。得意先未選択時はカード内に案内メッセージ（ファイル選択は非表示）。

#### 4.1.2 レイアウト

- 検索条件・操作は上部固定。**比較結果表のみ** `.db-table-wrap` 内で縦スクロール。
- `body.portal-app-page` でビューポート高に収め、ページ全体は縦スクロールしない。
- 表ヘッダは `position: sticky`。
- 得意先セレクトと開始日・終了日入力は **同じ高さ**（40px）。

#### 4.1.3 比較結果一覧列

| # | 表示名 | 編集 |
|---|--------|------|
| 1 | 受領データID | 不可 |
| 2 | 結果（`〇`/`△`/`×`） | 可 |
| 3 | 品目番号(MARI) | 不可 |
| 4 | 売上計上日(MARI) | 不可（支給品は受入日） |
| 5 | 売上実績数量(MARI) | 不可（支給品は受入数） |
| 6 | 得意先指定納品場所コード | 不可 |
| 7 | 品番(取引先) | 不可 |
| 8 | 納入月日(取引先) | 不可 |
| 9 | 納入数(取引先) | 不可 |
| 10 | キャンセル数(取引先) | 不可（**完成品のみ**表示） |
| 11 | 取引先名 | 不可（**支給品のみ**表示） |
| 12 | 備考 | 可 |

#### 4.1.4 ソート・メッセージ

- 列ヘッダクリックで昇順/降順（`sort`, `dir` クエリ）。
- **表示・比較直後**の初回ソート: **結果** 列昇順（`×` → `〇` → `△`）。
- 比較・登録・更新 **成功時は画面上部メッセージなし**（未登録は見出し「（未登録）」）。エラー・警告のみ表示。

### 4.2 受入/納品場所設定画面（SCR-02）

- 左: **得意先追加**、右: **得意先一覧**（2 列 Grid。左最大 380px）。
- 行クリックで `<dialog>` ポップアップ: 除外設定、完成品は直送先得意先コード、支給品は子取引先表、受入/納品場所（品番）表。
- **登録済み得意先コードは変更不可**。削除時は比較結果・取込履歴も連鎖削除。
- 完成品一覧列: 得意先コード・得意先名・直送先得意先コード・除外・受入/納品場所。
- 支給品一覧列: 得意先コード・得意先名・子取引先・除外・品番。
- 受入/納品場所（品番）表: **No.**・場所/品番・操作（削除のみ、操作列 120px 固定）。

---

## 5. ファイル取込仕様

### 5.1 共通

- 文字コード: **Shift_JIS / CP932** 第一候補。UTF-8 も受け付け可。
- 区切り: カンマ。

### 5.2 完成品（`.csv`）

| 項目 | 内容 |
|------|------|
| ヘッダー | あり |
| 必須列 | `カードNO`, `品番`, `納入月日`, `納入数`, `納入取消数` |
| 除外 | `カードNO = "S"` の行は対象外 |
| 取込項目 | 品番、納入月日、納入数、納入取消数 |

### 5.3 支給品（`.txt`）

| 項目 | 内容 |
|------|------|
| ヘッダー | なし。ダブルクォート囲み行あり |
| 2列目 | 登録済み **子取引先** `vendor_code` または親 **得意先** `customer_code` と完全一致 |
| 4列目 | 品番（TRIM 後 **10桁**）。品番設定と照合（除外フラグに従う） |
| 3列目 | 日付から `/` 除去、右4桁を `MMDD` |
| 6列目 | 数量（小数可）。0以下は除外 |
| キャンセル数 | 0 固定。取引先名は 2列目 |

---

## 6. Oracle（MARI）データ取得

### 6.1 完成品

**テーブル**: `T_SHIP`, `T_SHIP_ODR`, `T_SALES_TEMP`

| 条件 | 内容 |
|------|------|
| 取引先 | `direct_delivery_customer_code` 優先、なければ `customer_code` |
| 期間 | `SHIP_DATE` / `SALES_DATE` BETWEEN 開始日 AND 終了日 |
| `T_SALES_TEMP` | `SHIP_SEQ_NO IS NULL` |
| 数量 | `SHIP_QTY` / `SALES_QTY` **> 0** の明細行のみ |
| 集約 | **`SUM` / `GROUP BY` しない**（明細行のまま。検証: `scripts/verify_mari_shipment_aggregation.py`） |
| 納品場所 | 登録済み受入/納品場所と `exclusion` に従い `IN` / `NOT IN` |

**取得項目**: `item_cd`, `ship_date`, `ship_qty`, `delivery_place`（`CUST_DESINATED_DLV_LOC_CD`）

### 6.2 支給品

**テーブル**: `T_ACPT_RSLT`, `T_RLSD_PUCH_ODR`, `M_VEND_CTRL`

| 条件 | 内容 |
|------|------|
| 取引先 | PostgreSQL 登録得意先の **子取引先** コード一覧で `VEND_CD IN (...)` |
| 期間 | `ACPT_DATE` BETWEEN 開始日 AND 終了日 |
| 品番 | `ITEM_CD` 末尾5桁除去（LENGTH>10 時）。TXT 側正規化と一致 |
| 場所除外 | **MARI SQL には適用しない**（旧実装準拠）。TXT 取込側のみ品番設定で絞込 |

---

## 7. 比較ロジック

### 7.1 一致判定（3項目）

| 項目 | 正規化 |
|------|--------|
| 品番 | MARI の `-` 除去。支給品は末尾5桁除去ルールを TXT/MARI で統一 |
| 日付 | MARI 日付 → `MMdd` と受領 `納入月日` を比較 |
| 数量 | 数値同値（`0` は空表示。NULL は `0` 扱い） |

3項目一致 → **`〇`**。不一致・片側のみ → 初期 **`×`**。ユーザーが **`△`** に手動変更可。

### 7.2 表示処理（取込後）

1. MARI 行を一覧に追加。
2. 保存済み結果を読込み、品番・日付・数量一致行へ受領側情報を反映。
3. 受領書各行を MARI と比較。一致 MARI 行は `〇` + 受領側列をセット。
4. 未一致 MARI 行は `×`。未一致受領行は MARI 列空の `×` 行として追加。

### 7.3 未完了データの引き継ぎ

開始日より前の **`receipt_flag != 1`（`〇` 以外）** の保存済み行も次回一覧に含める（旧 `SelectReceiptData` 準拠）。

---

## 8. PostgreSQL 永続化

完成品・支給品で **テーブル分離**（`code_type` は使用しない）。

### 8.1 完成品モデル

`FinishedProductReceiptSupplier`, `FinishedProductReceivingSetting`, `FinishedProductFileImport`, `FinishedProductComparisonResult`

- `customer_code`（3桁・一意）、`direct_delivery_customer_code`, `exclusion`

### 8.2 支給品モデル

`SuppliedPartsReceiptSupplier`, `SuppliedPartsSubcontractor`, `SuppliedPartsReceivingSetting`, `SuppliedPartsFileImport`, `SuppliedPartsComparisonResult`

- 子取引先: `vendor_code` / `vendor_name`（Oracle 4桁マスタから選択・名称は登録時保存）

### 8.3 比較結果 `receipt_flag`

| 値 | 表示 |
|----|------|
| 0 | `×` |
| 1 | `〇` |
| 2 | `△` |

---

## 9. 出力仕様

### 9.1 CSV

| 項目 | 内容 |
|------|------|
| ルート | `GET /app/production/receipt-comparison/export` |
| ファイル名 | `検収書比較結果(完成品)_YYYYMMDDHHMMSS.csv` / `検収書比較結果(支給品)_...` |
| 文字コード | CP932 基本 |
| `Content-Disposition` | RFC 5987 `filename*`（UTF-8） |
| 内容 | 一覧表示列と同一（未登録プレビュー含む） |

### 9.2 印刷

初期実装では対象外（ブラウザ印刷/PDF は将来拡張）。

---

## 10. エラー・警告（代表）

| 状況 | メッセージ例 |
|------|----------------|
| 区分不正 | 正しい比較区分が読み込まれませんでした |
| ファイル未指定 | 受領書データを選択してください |
| 拡張子不正（完成品） | 受領書データ(csv)を選択してください |
| 拡張子不正（支給品） | 受領書データ(txt)を選択してください |
| 得意先未選択 | 得意先を選択してください |
| MARI 失敗 | MARIデータの取得に失敗しました |

---

## 11. 権限・メニュー

| 項目 | 内容 |
|------|------|
| メニューキー（親） | `receipt-comparison` |
| 子リンク | `receipt-comparison-finished-product`, `receipt-comparison-supplied-parts` |
| グループ | `production`（生産管理） |
| 比較画面 | 生産管理グループまたは管理者 |
| 設定画面 | **管理者のみ** |
| お気に入り | 完成品/支給品各画面タイトル横 |

---

## 12. 非機能要件

| 項目 | 内容 |
|------|------|
| SQL | パラメータバインド必須（文字列連結禁止） |
| Oracle | 更新禁止 |
| テスト | [検収書比較_テスト仕様書.md](./検収書比較_テスト仕様書.md)。CI では Oracle モック |
| クリーンアーキテクチャ | `tests/test_clean_architecture.py` で `receipt_comparison` を検証 |

---

## 13. 改訂履歴

| 版 | 日付 | 内容 |
|----|------|------|
| 3.0 | 2026-06-23 | **資産棚卸結果・5年9組と同一章構成へ再編**。実装済み内容を統合。`usecase_*.py`・テーブル分離・`type` クエリを明記 |
| 2.x | 〜2026-06-24 | 旧 WinForms 解析版からの段階的反映（支給品子取引先、明細行 MARI、クリーンアーキテクチャ移行等） |
| 0.1 | 2026-06-04 | 初版（旧受領書比較 MARI 解析） |

---

## 付録A. 入荷なし出荷検証（別機能・参考）

5年9組と同一 Oracle を用いた **検証スクリプト** `scripts/verify_post_receipt_shipment_count.py` の仕様。本番機能は **在庫発注アラート** として別途実装。詳細は [在庫発注アラート_機能仕様書.md](../在庫発注アラート/在庫発注アラート_機能仕様書.md) および旧版付録A を参照。
